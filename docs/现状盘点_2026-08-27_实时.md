# model_data 现状盘点（实时，2026-08-27 01:1x）

> 本文件由实时审计生成，反映 `model_data_v2.jsonl` 的真实当前状态。
> 审计脚本：`temp/_live_audit.py`（纯标准库，可重复运行）。
> 注意：本文件比《全库状态与下一步_2026-08-25.md》更新——主库文件 mtime 为 08-27 01:13，
> 晚于状态文档的 08-26 21:20，说明状态文档之后又发生了一次入库/修正（见下方「与文档的差异」）。

---

## 一、一句话结论

**记录层面「模型不缺」：roster 定义的采集目标宇宙（in_v1 333 + to_add 26 = 359 个模型）已全部入库，0 缺失。**
**剩余工作全是「字段完整度/质量」，不是模型数量**：333 个 in_v1 模型里仍有 140 个字段稀疏（fill_score≤2）；全库 950 条在 self_reported / pricing / effective-context / positioning 等维度填充率仅 15–22%。
校验门禁：**ERROR 0 项 / WARN 119 项**（WARN 均为非阻塞的规范性提醒）。

---

## 二、主库健康度（model_data_v2.jsonl）

| 指标 | 实测值 |
|---|---|
| 记录数 | 950（无重复 model_id，无坏 JSON） |
| schema 版本 | 1.1 |
| 厂商数 | 273（Top：Alibaba 76 / OpenAI 70 / Anthropic 44 / Mistral 41 / Google DeepMind 38 / DeepSeek 32 / Google 29 / Meta 27 / NVIDIA 25 / Microsoft 20 / xAI 19） |
| 校验 ERROR | **0** |
| 校验 WARN | **119**（非阻塞，详见第七节） |
| verification_status | 待验证 938 / 已过期 8 / 已验证 3 / 存疑 1（≈99% 仍未复核） |
| vendor=unknown 残留 | 4 条（hierarchical-lm / recontra-categorized / community-research-earth-digital-intelligence-twin / yue-ai，按红线刻意保留） |

---

## 三、「还差多少模型」——按采集目标宇宙（roster）

roster 共 506 条：`in_v1=333` / `to_add=26` / `out_of_scope=147`。
采集目标宇宙 = in_v1 + to_add = **359 个模型**。

| 类别 | 总数 | 已在主库 | 缺失 |
|---|---|---|---|
| to_add（原缺旗舰） | 26 | **26** | **0** |
| in_v1（v1 已有） | 333 | **333** | **0** |
| **合计** | **359** | **359** | **0** |

➡️ **记录层面的「缺失模型数 = 0」。** 连原文档称「还差 3 个 to_add」的 3 个也已入库（见差异说明）。

---

## 四、真实剩余工作量：in_v1 字段完整度

in_v1 模型虽已全部入库，但 M 型字段补全只做了一部分。fill_score（0 稀疏–6 完整，覆盖 positioning/上下文/定价/多模态/自报分 六维）：

| fill_score | 模型数 |
|---|---|
| 0 | 111 |
| 1 | 21 |
| 2 | 8 |
| 3 | 50 |
| 4 | 56 |
| 5 | 62 |
| 6 | 25 |

- **稀疏（≤2）：140 / 333（42%）** ← 这是 Batch7+ 的真正 backlog
- 良好（≥3）：193 / 333（58%）

---

## 五、全库字段填充率（n=950）

| 维度 | 填充 | 占比 |
|---|---|---|
| basic_info.release_date 非空 | 942 | 99.2% |
| architecture 参数量（任一）非空 | 582 | 61.3% |
| architecture.context_window_tokens 非空 | 229 | 24.1% |
| architecture.context_window_effective_tokens 非空 | 67 | 7.1% |
| pricing 有实际价格 | 208 | 21.9% |
| modality.input.text 有判定值 | 222 | 23.4% |
| basic_info.positioning 非空 | 180 | 18.9% |
| benchmarks.self_reported ≥1 条 | 144 | 15.2% |
| benchmarks.arena_elo ≥1 条 | 164 | 17.3% |
| benchmarks.independent ≥1 条 | 301 | 31.7% |

> 最薄弱项：有效上下文 7.1%、自报分 15.2%、定位 18.9%、定价 21.9%、多模态 23.4%。
> 这些不是「缺模型」，而是「已有模型缺字段」。

---

## 六、范围外 / 未纳管

- **404？不，是 444**：主库 950 条里，**有 444 条 model_id 不在 roster 任何状态中**（既不是 in_v1 / to_add / out_of_scope）。
  它们是 v1 时代入库、但未被 roster 逐条纳管的变体/版本。不属于「缺失」，但属于「未纳入工作清单」，后续若要全量治理需先扩 roster 覆盖。
- roster 全部 506 条均已入库（0 条 roster 模型漏库）。

---

## 七、WARN 119 项构成（非阻塞）

与历史趋势一致（Batch6 末 92 → 现 119，+27 主要来自新增 3 个 to_add 入库带来的自报分 source_type 提醒 + 有效上下文 notes 提醒）：
1. 自报分 source_type 未体现「自报」字样（规范性）；
2. 标称上下文已填但有效上下文为空且 notes 缺「标称值，有效上下文待测」短语；
3. 填了有效上下文但 notes 未注明独立测试方法与来源。
**均不触发门禁，可在后续 Batch 修复脚本中统一规范化。**

---

## 八、与《全库状态与下一步_2026-08-25.md》的差异

| 项 | 文档末态（Batch6，08-26 21:20） | 实时（08-27 01:13） |
|---|---|---|
| 主库记录数 | 950 | 950（一致） |
| to_add 入库 | 称 23 个，剩 3 个 | **26/26 全入库** |
| 校验 WARN | 92 | **119** |
| verification_status=已过期 | 未提及 | **8 条**（疑似后续修正引入） |

说明：主库文件在状态文档之后被再次写入，3 个 to_add + 若干字段修正已落地，文档尚未同步。建议下次收尾时把本文结论回写主状态文档。

---

## 九、下一步建议（按性价比）

1. **补 in_v1 稀疏 140 个**（Batch7+，单厂商清空策略，沿用 Batch4/5/6 的 14 条硬规范任务书 + 顺序 merge 模板）——这是最实在的「还差」部分。
2. **扩 roster 覆盖 444 条未纳管记录**，确定要不要纳入 M 型补全范围。
3. **清 WARN 119**（一次规范化脚本，非阻塞）。
4. **推动 verification_status 复核**（941/950 仍待验证/过期/存疑，目前全是诚实但未核实）。
5. **回填最薄弱字段**：有效上下文（7.1%）、自报分（15.2%）、定位（18.9%）、定价（21.9%）、多模态（23.4%）。
