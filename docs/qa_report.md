# 阶段 3 质检报告（model_data v2）

> 执行者：`workbuddy-05`（本机采集主 agent）
> 执行时间：2026-08-29
> 依据：`multi_agent_plan.md` §5「阶段 3 · 质检与验收」、`COLLECTION_PLAN_v2.md` §6 验收标准、`WORKBUDDY_AGENT_GUIDE.md`
> 统计口径脚本：`scripts/qa_stats.py`（填充率）、`scripts/diff_incoming_db.py`（差异/冲突）
>
> **⚠ 文中 `temp/*.py` 指针不入库**：`.gitignore` 排除了 `/temp/`，那些是 D2–D18e 各轮整改的本机一次性脚本，
> 克隆仓库看不到它们（拍板于 2026-08-30，与 `WORKBUDDY_AGENT_GUIDE.md` 开头同一声明）。
> 判据、负对照数字与验收结论都写在正文里，复核请按这些做；常驻工具只有 `scripts/` 下那几个。

> **【同日补记 2026-08-29 起 · 整改轮 D1–D18e】** 本报告的质检结论之后，同日又跑了一轮整改，读本文时注意十九处时效性：
> 1. §1 的 **ERROR 0 是在旧门禁下取得的**——旧门禁不查 `pricing.confidence` 枚举、不查 `knowledge_cutoff` 格式、
>    不查 `source_type` 与价格值是否自相矛盾，也不查「无价却填了币种」。四项补齐后为 **ERROR 0 / WARN 689**，
>    此后逐轮为 D11 **703**、D12 **706**、D13 **474**、D14 **469**，~~**现库（D15 改完）仍是 ERROR 0 / WARN 469**~~
>    **→ 续：D15 / D16 / D17c 三轮基线均为 469 未变，D18 起降为 33（= 常量段 16 + 规则 6.2 主键撞车 17），见第 15 项；**
>    ~~**D18b 只加门禁规则 6.3（来源类型栏整栏写等级值即报，命中 607 条）、数据一字未动 → 现库 ERROR 0 / WARN 640（= 常量段 16 + 规则 6.2 的 17 + 规则 6.3 的 607），见第 16 项**~~
>    **D18c 把其中 518 个条目改了数据（乙档 395 条等级值迁进可信度栏 + 甲档 123 条清冗余）→ 现库 ERROR 0 / WARN 122（= 常量段 16 + 规则 6.2 的 17 + 规则 6.3 剩的 89），见第 17 项**
>    **D18d 又改了 405 个条目的数据（剥掉来源类型栏里当前缀写的等级值，如 `T0 官方自报` → `官方自报`），但 WARN 仍是 122 —— 改前改后两份门禁报告除标题里的文件名外逐字节相同，这轮对门禁完全不可见，价值是语义的，见第 18 项**
>    **D18e 反过来：数据一字未动、只改门禁 —— 新增 WARN 级规则 6.4 照「等级值当前缀写进来源类型栏」这类（D18d 剥净后现库命中 0，纯防回归），所以 WARN 仍是 122。两轮都是 122 不变但原因相反：D18d 是「改了数据而门禁照不到」（数据 md5 变、门禁 md5 不变），D18e 是「改了门禁而现库本来就干净」（数据 md5 不变、门禁 md5 `f70bcb83…` → `6ea4de42…`），见第 19 项**
>    （中间多出的 17 组是规则 6.2 照出的主键撞车待复测清单，恒定不变；详见第 8、9、10、11、12 点）。
> 2. §3.2 第 2 点与 §5-6 关于「开源权重却有定价」的判定**已被推翻**，红线 5 同日改按
>    「厂商有无自有官方 API 刊例价」判定，详见 `multi_platform_subagent_guide.md` §5 红线 5 与 `WORKBUDDY_AGENT_GUIDE.md` §16。
> 3. 门禁现行口径以 `scripts/validate_model_data.py` 为准，**本文与规范文档都只是它的说明，不是判据本身**。
> 4. **主库记录数已变：950 → 940**。采集人在 `meta.verification_status` 标了 `存疑` 的 10 条经逐条复核确认查无依据，
>    经用户拍板全部移出主库，原样存 `docs/unconfirmed_models.jsonl`，对应采集文件移入 `incoming/models/_quarantine/`
>    （机制见 `WORKBUDDY_AGENT_GUIDE.md` §17）。花名册口径因此重基线为 **主库 692 + 隔离档 10 + 缺失 0**，
>    这 10 条**不是漏采、不要重采**。本文正文所有 950 / 678 一类计数均为整改前快照，未回填。
> 5. **本文 §3 / §3.1 的 `independent` 与 `arena_elo` 两行是「破坏后」快照**：本轮「补合并 277 条」用
>    `--on-array replace` 静默抹掉了 81 条记录 / 215 个已采集跑分条目，把这两个维度的填充率打了下来。
>    **D8 已回补并结案**：累计净回补 **82 条记录 / 207 个条目**（`independent` 178 + `arena_elo` 29；另 9 条
>    `self_reported` 经核对属合法 schema 升级，不计损失），回补后花名册 `independent` 18.2% → **28.3%**、
>    `Arena Elo` 6.4% → **7.8%**，全库 `independent>0` 33.8%。详见 §3.2 第 4 点与 `WORKBUDDY_AGENT_GUIDE.md` §18。
> 6. **`pricing.currency` 口径已拍板并归一（D7）**：无价即无币种，六价键全 null 的 323 条已统一为 `null`，
>    门禁新增规则 4.3 防回归；§6.2 里「currency 口径未定」一行已结。根因是 `prompt.md` 原文写着
>    「`currency` 默认 `"USD"`」，该字段说明与三处采集侧文档同日一并改正。
> 7. **`benchmarks` 条目写法已全部归一（D9 + D10）**：主库原本 canonical 加**四种**非 canonical 写法并存 ——
>    legacy 只有 `name` 的 **1293 条**（self_reported 1241 / independent 50 / arena_elo 2，涉及 156 条记录），
>    外加 `benchmark_name` 30 / `metric_name` 23 / `arena_elo` 误用 `benchmark` 4（共 **57 条 / 11 记录**）。
>    这不是格式问题：合并去重主键对这类行永远算出 `("None","None")`，同一次测量会静默并存两份
>    （D8 回补撞出的 5 条同名冲突即由此来）。两轮全部机械改名为 canonical 主键
>    （前两个数组 → `benchmark`，**`arena_elo` → `sub_benchmark`**），改后反向核对逐字节可还原、值零改动，
>    现库 **ERROR 0 / WARN 689**（D11 起 **703**、D12 起 **706**，见第 8、9 点）、**缺 canonical 主键 0**。
>    规则 6.1 按「**先清数据、再收紧检查**」的顺序在 D10 之后扩成「缺 canonical 主键即报」，
>    负对照用两个改前备份实测 **746 = 689 + 57**、**2039 = 689 + 1293 + 57**。
>    归一时揭出的**同名基准分数冲突**（全库精确主键重复 22 个数组 / 39 组）已在同日 **D11 结案**，见第 8 点。
> 8. **39 组主键撞车已结案（D11），WARN 基线 689 → 703**：先逐条摊开量化，发现主要毛病**不是「两个来源分数打架」**
>    而是**主键不足以标识一次测量**——38/39 组内 `confidence` 完全相同（「取高 confidence」只能裁决 1 组），
>    35 组同名不同分里 **34 组整组只有一个 `source_url`**。按「notes 能否复原区别」分五档处置：
>    A 档 15 组把 notes 里写明的评测设置填进 `config`、B 档 5 组把 `score_type` 口径追加进 `config`、
>    C 档 1 组（`sber:fred-t5-xl` RSG 的 10 条子任务）拆进 `benchmark` 名、D 档 4 组纯重复删后到的一条、
>    **E 档 14 组 notes 也分不出谁是谁 → 不动，由新规则 6.2 报出来当待复测清单**（9 组集中在
>    `microsoft-nvidia:megatron-turing-nlg-530b`，要分开只能回原表重读，属重采范围）。
>    全库 `score` 零改动，条目 5659 → 5655。本轮同时**第一次写下 `config` 的语义边界**（见指南 §20）。
>    负对照三份备份：D9 前 **2067** = 689 + 1350(6.1) + 28(6.2)、D11 前 **728** = 689 + 39、现库 **703** = 689 + 14。
>    **仍待拍板的两项均已在同日 D12 结案**（含本点里那两处口径修正：合并主键一直是 `(benchmark, config, date)`，
>    本文与指南 §20 原文漏写了 `date`）：来源名已迁出 `config`、`independent` 主键已扩入新字段 `source_site`；
>    `score_type` 是否进主键改为「已量化后否掉」。详见第 9 点与指南 §21。
> 9. **`config` 写来源名已整改（D12 + D12b），WARN 基线 703 → 706**：第 8 点的「7 组」只是 residual 扫描里
>    「同分、剥掉来源名即可合并」这一**子情形**，整个现象的范围是 **92 条条目 / 21 条记录**。判定法必须自证据化——
>    `config` 的括号段只有出现在**本条自己的**
>    `source_url` 主机名/路径或 `source_type` 里才算来源名；用「含括号的英文段」这种启发式会误伤 **314 条**合法配置。
>    处置：`independent` 新增 `source_site` 字段并入合并主键（`benchmark, config, source_site, date`），`config` 只留评测设置；
>    随后又发现该判定自身的结构性盲区 —— `Artificial Analysis（镜像站）` 一族的括号名永远不等于自己的主机名，
>    第二遍补迁 **59 条 / 14 记录**（镜像属**读取路径**不是测量身份 → `source_site` 写站点本身、镜像站名写进 `notes`）。
>    两轮净改动 **145 条条目 / 23 条记录，全部在 `independent`**（`config` 145 + 新增 `source_site` 145 + `notes` 追加 1），
>    **`score` 改动 0**，记录 940→940、条目 5655→5655，两轮各自反向还原逐字节相等。
>    **代价如实登记**：跨来源并存不再被主键吞掉后，规则 6.2 照出 3 组「同一次 AA 测量被两个镜像各抄一次」
>    （GPQA 0.81 vs 0.87、MMLU-Pro 0.889 vs 0.9 是分歧，SWE-bench 0.809 vs 0.809 是同分真重复），
>    精确主键重复组数 14 → **17**，WARN → **706**。这不是回归，是 E 档机制按设计工作。详见指南 §21。
> 10. **上下文口径已废止并重写（D13），WARN 基线 706 → 474**：用户 2026-08-30 拍板——「有效上下文未经独立测试
>    一律填 `null`」这条口径来源不明且与全库实际背离，**废止**，173 条抄标称的记录**原样保留**（厂商标称值不算
>    说谎，差别只在长文尾端效果，而这一栏的含义是「支持到多长」不是「多长以内效果不掉」）。
>    新口径见 `执行细则.md` §2、`prompt.md` 字段字典、`agent_prompt_per_model.md`、指南 §14：有效栏不要求独立实测，
>    出处写 notes，**但不得大于标称栏**。
>    门禁随之删掉为该口径服务的两条 WARN（全库实测命中 **127 + 105 = 232** 条），WARN 706 → **501**；
>    替换成一条新的结构不变量 WARN「有效上下文大于标称上下文」，命中 **27 条**——这 27 条是厂商
>    「原生 X / 可扩展到 Y」被填反（Y 进了有效栏、X 留在标称栏，`deepseek:deepseek-chat` 的标称 4,000 即此类），
>    按方案 A 归位：较大值进标称栏、有效栏置空、备注补归位说明。
>    **一处例外**：`deepseek:deepseek-v3:0324` 的较大值 163,840 按其自己的 notes 是**第三方站点转述、非官方直采（T3）**，
>    而 131,072 是官方 README `Context Length 128K` 的 T0 直采值 → 搬大值进标称栏等于用三方数冒充官方数，
>    违反红线，故该条**标称栏保持 131,072**，只清空有效栏，数值与出处全部留在 notes。
>    净改动 **27 条记录**（`context_window_effective_tokens` 27、`context_window_tokens` 26、`notes` 27；
>    备注里 `context_window_*=` 这种字段-值绑定文字一并改名，避免改完自相矛盾——D4 清过的那一类），
>    记录 940→940、条目 5655→5655、`score` 零改动，反转可逐字节复原。有效上下文非空 208 → **181**。
>    现库 **ERROR 0 / WARN 474**（= 501 − 27 条倒挂；其中规则 6.2 的 17 组不变）。
> 11. **主库范围收窄为「只记录模型」（D14），940 → 933 条**：用户 2026-08-31 拍板「数据库里不要训练框架什么的，
>    我们只记录模型」。移出判据**只认记录自己的声明**——`architecture_type` 值或 `notes` 里明说对象是
>    训练系统 / agent 系统 / 算法框架 / 分布式训练技术，共 **7 条**：`allen-institute-for-ai:codescientist`
>    （备注原文「由于它不是模型，所有 architecture/benchmark/pricing 字段均设为 null」）、
>    `bytedance-peking-university:megascale`（值即 "Training system/infrastructure (not a model)"）、
>    `deepmind:alphaevolve`（「是系统而非单模型」）、`factory:code-droid`（「agent 系统而非基础模型」）、
>    `imperial-college…:omni-epic`（"Not a model（开放式算法框架…）"）、`nous-research:distro`
>    （「不是模型，是…开源分布式训练技术」）、`unknown:community-research-earth-digital-intelligence-twin`
>    （「CREDIT 是框架而非单一模型」）。原文**逐字节**存 `docs/non_model_records.jsonl`，5 个采集文件移入
>    `incoming/models/_out_of_scope/`；验收 = 新库行 + 归档行的多重集与原文件逐字节相等 + 独立复算
>    `md5(排序(主库+归档)) == md5(排序(改前备份))`，条目 5655 → 5642（带走 13 个跑分条目），
>    WARN 474 → **469**（归档 7 条自身贡献 5 条，469 + 5 = 474 严丝合缝）。
>    **两条如实交代的方法局限**：① 宽松关键词不可用——按「框架/系统/平台/pipeline」扫命中 79 条，
>    逐条读下来几乎全是误伤（「训练框架 hai-llm」是真模型的训练工具、「非模型上下文上限」是上下文口径说明、
>    「专为代理式 AI 多智能体系统设计」是用途），故改用窄判据；② **窄判据只抓得到自己声明的记录**，
>    一个采集者没写「我不是模型」的系统级条目扫不出来。乙档 3 条（`xai:grok-4-heavy` 值为
>    "Multi-agent orchestration"、`inspur:haiyue` 值为「复合式 AI…多智能体协同架构」、
>    `huawei:pangu-5-0` 备注称「系列级发布而非单一模型」）经用户点定**留在库内**。
>    花名册口径因此变为 **主库 685 + 非模型 7 + 存疑隔离 10 + 缺失 0 = 702**，这 7 条同样**不得当「漏采」重派**。
> 12. **`architecture_type` 已拆成两栏（D15），933 条不变、WARN 仍 469**：用户 2026-08-31 两轮拍板 —— 先定
>    「拆成两栏：稀疏性枚举 + 主干结构枚举」，再定「判不动时**只许借力本条自己的备注**，不许用通识反推」。
>    动因是一栏里塞了 **190 种自由写法**（改前 933 条备份实测，越出旧枚举的 **186 种 / 289 处**；例句均为库内逐字原文：
>    `Dense Transformer` 26 处、`Transformer (decoder-only, GQA, RoPE, RMSNorm, SwiGLU)`、
>    `Sparse MoE — MLA + MTP + 高稀疏比（256 路由专家 + 1 共享专家，每 token 激活 8 专家）`…
>    稀疏性与骨干混写，机器无法按任一维度聚合）。现口径：`architecture_type` 只填 `Dense/MoE/Hybrid/Unknown`，
>    主干结构进新栏 `architecture.backbone_type`（11 值枚举），**原文一字不丢**照抄进
>    `architecture.notes` 的 `；原架构表述：「原话」`。范围 **300 条 = 越界 290（289 条越出旧枚举 + 1 条值为 `null`）
>    + 纯 `"Hybrid"` 10**，实写 **299 条**：唯一未动的是 `xai:grok-3-mini:base` —— 它的 `architecture_type`
>    本就是 `null`，且备注原文写着「`architecture_type=null`：xAI 闭源未公开架构细节」，
>    拆栏只处理「有内容待归类」的条目；给一条已自陈未披露的记录补
>    `backbone_type: "Unknown"` 等于把「没采到」改写成「已核对并确认无法归类」，还凭空多出一个键，**故整条不动**。判定顺序 ① 值文本（先剥否定）→ ② 本条 `notes` 明文 →
>    ③ 裸 `"Hybrid"` 归主干栏、稀疏性 Unknown → ④ 5 条人工裁定表 → 否则两栏 Unknown；
>    **判定表先逐条给用户过目才动数据**，87 条借力全部打印命中词 ±28 字上下文、39 处命中被闸门挡掉、
>    5 条人工裁定各自引用本条原话（含 `ant-group:bailing-pro:1120` —— 它的 MoE 归属其实是后继型号
>    Ling-2.6-flash，**兄弟型号的声明不得算到本条头上**）。过程中修掉的判据缺陷均有样本可查，见指南 §24。
>    门禁新增 **1.1 / 1.2** 两条枚举包含性检查，**刻意停在 WARN**：`docs/non_model_records.jsonl`（越界 **4 条**）与
>    `docs/unconfirmed_models.jsonl`（**2 条**）按行 byte-exact 留存、仍是拆栏前的自由文本，判 ERROR 会失真历史验收信号；
>    更直接的是 `incoming/models/` 平铺的 310 个采集文件里 **158 个各含 1 条**拆栏前写法，而门禁在合并时**逐文件**跑、
>    要求 ERROR=0（指南 S5），判 ERROR 会把后续合并整批挡住。
>    验收：反向还原逐字节等于改前文件、备份 md5 与改前 HEAD 相同、记录 933→933 / 条目 5642→5642 / `score` 零改动、
>    键序 `backbone_type` 紧跟 `architecture_type` 全部 299 条无一例外；**WARN 改前 758 → 改后 469**
>    （差额 289 正是新规则 1.1 打在旧数据上的命中数），规则 1.1/1.2 现库命中 **0**。**遗留**：633 条未参与拆栏
>    （值本就合规但主干信息为零，其中 373 条 `Unknown` 若放开 notes 借力可再捞一批，本轮按拍板范围不做）、
>    9 条两栏皆 `Unknown`、`backbone_type` 目前无任何下游脚本消费。机制与判据见指南 **§24**。
> 13. **`pricing.free_tier` 四种形状已归一（D16），933 条不变、WARN 仍 469**：改前同一字段并存
>    `null 718 / 对象 134 / 纯文字 52 / 裸布尔 29` 四种形状，下游按 `free_tier.available` 读取时后两类取不到值。
>    **根因是文档与门禁互相矛盾**（与第 6 点 D7 的 `currency` 同病）：`prompt.md` 字段说明原文写着
>    「如 `"每月 100 万 token 免费"`」教的是字符串写法，而门禁结构漂移检查的 `cmp_block` 对非 dict 直接 return，
>    于是采集照文档写 → 门禁不报 → 漂移静默累积到 81 条才被发现。
>    两轮拍板：第 18 轮「统一成结构化对象」（键序固定 `available, rpm, rpd, tpm, notes`）；
>    ~~第 19 轮「任一途径免费即 true」+ 限定「标注的是**存在免费渠道**，免费与否仍看官方 API 定价」~~。
>    ~~**`available` 的真实语义是「存在某条免费途径」而不是「这个模型免费」**~~（**这两句的宽口径已被第 20 轮拍板推翻**，
>    现为「vendor 官方 API 当前是否有免费额度」的窄口径，见第 14 点）；**不变的部分**是它**不得参与也不得抵消价格判断**——
>    下游统计**严禁**用 `available` 反推价格，是否免费只看 `pricing.input/output/cached_input/batch_*` 及各自 `confidence`；
>    `available=true` 时 `notes` 必须写清是哪条渠道，判不动就 `null`（偏差方向是少填）。
>    第四轮逐条拍板四个分叉：随订阅提供/计划内含额度 → `null`；官方 API 试用 key → `null`；
>    已过期或随模型下线失效的渠道 → `false`；同句既有订阅背景又有现存免费渠道 → `true`。
>    范围由 81 条扩到纳入 51 条 dict 候选同口径重判（其中 9 条分三组提请裁定）；判据按**子句切分**逐句独立判再聚合，
>    **20 条用户裁定由规则独立复现 20 / 覆盖 0**，据此认为规则泛化成立才用于 dict 候选。
>    实写 **84 条**（形状迁移 81 + dict 改 `available` 3）：记录 933→933、条目 5642→5642、`score` 零改动、
>    键序 215 条全一致、`available` 分布 `True 120 / False 36 / null 59`（**D16 快照，已被第 14 点的三遍改判覆盖，
>    现为 是 13 / 否 92 / 空 110**），反向还原逐字节复原、备份 md5 与改前 HEAD 相同。
>    门禁新增规则 **4.4**（非对象形状即 WARN），**负对照用改前备份实测命中 81**（550 = 469 + 81），改后命中 0。
>    四份采集文档同日补齐，否则下批采集会照旧写回字符串。机制与判据见指南 **§25**。
>    **遗留两条（均已由第 14 点的 D17 弧线关闭）**：① 5 条 `available=null` 未纳入扫描，其中 `mistral:magistral-small-1-0`「模型已从官方 API 退役」
>    按分叉 C 应为 `false`，因 notes 未命中扫描判据而漏网，是唯一一条真漏（**已改判 `false`**）；② **一处口径张力待用户定夺** ——
>    `google:gemini-3-flash`「Gemini APP 默认免费使用」判 `true`，而另两条官方 App/Web 免费对话
>    （`deepseek-v4-pro` 的 chat.deepseek.com、`gemini-3-1-pro-preview` 的 Gemini App 免费档）判 `null`，
>    三者同构却结论相反，源于两次拍板间的细微漂移（**第 20 轮窄口径拍板消解：官方 App 一律不算，三条归一为 `null`**）。
> 14. **`free_tier.available` 语义收窄，同一条口径分三遍走完（D17 / D17b / D17c）**：第 20 轮把该字段从 D16 的**宽口径**
>    （「存在某条免费途径：App / Web / Playground / 新用户赠送额度…任一」）收窄为**窄口径**（「vendor 官方 API 接口
>    当前是否有免费额度」），排除官方 App / 网页端 / Playground、消费端订阅内含额度、开源权重下载与自托管、
>    第三方托管免费档（OpenRouter `:free` / Cerebras / HF serverless）、官方限速 API 试用 key，且**只记当前状态不记历史**。
>    第 21 轮追加四项裁定：**Q1** 平台级额度不算到本条头上（23 条阿里云「开通百炼后 90 天内各模型有免费额度（以控制台为准）」
>    → `null` + 登记待重采）；**Q2** legacy ≠ retired（从定价页移除或标 legacy、但未明示 retired 且可调用性未实测 → `null` 而非 `false`）；
>    **Q3** 窄口径不要求额度以 token 计量（`amazon-q-developer` 的「50 次 agentic 请求/月 + 1000 行代码/月」保 `true`）；
>    **Q4** vendor 自有 Free API endpoint 算（`nvidia:nemotron-3-ultra` 的 `integrate.api.nvidia.com/v1` 免费端点保 `true`，并补写原本为空的 notes 依据）。
>    **三遍各改判 67 / 45 / 3 条，累计 `available` 改判 115 处（是→否 48 / 是→空 59 / 空→否 8）、触及 120 条记录、notes 改 63 处**；
>    终态 **是 13 / 否 92 / 空 110**（D16 收尾时是 120 / 36 / 59），形状仍 `null 718 / 对象 215`、键序变体 1。
>    全程不变量：记录 933→933、跑分条目 5642→5642、`score` **零改动**、`model_id` 顺序一致、未改动行逐字节相同、
>    越界路径 0，**门禁 933 条 / ERROR 0 / WARN 469 三遍均不变**（规则 4.4 只查形状，215 条改后仍全是 dict，本轮无需新增门禁规则）。
>    **本轮最该被读到的不是数字，是判据的失明面**：六个匹配盲区（证据面只读一栏、`RETIRED` 漏日期插入、`API_FREE`
>    无锚点肯定词导致**否定失明**且还当了仲裁者去否决别的规则、`NO_API` 是 7 条短语手抄表、矛盾句检测器三重失明、
>    `PLAT` 平台话术表漏词序变体），加审计脚本自身的**量词作用域盲区**（把 google 两条的「Grounding 各 5000 次/月」
>    误当模型免费档，2 条误报登记为具名豁免而非悄悄放宽正则），再加**文档示例本身就是判据**（`prompt.md` 三处教的写法
>    过不了门禁或方向是错的，其中 436 条自报分的 `source_type` 缺「自报」正是抄自文档下拉表）。
>    **遗留登记**：26 条阿里云记录待重采官方 per-model 免费额度表；`open-mistral-nemo:2407`（`true`）与孪生条
>    `mistral-nemo-base:2407`（`false`，T0 退役表直采）事实对撞，已追加冲突登记、**原句逐字节保留**待重采定夺。
>    机制、判据与全部教训见指南 **§26**（含 §26.1 / 26.1b / 26.2 / 26.3 / 26.4 / 26.5 **六个**子节）。
>    其中「436 条自报分 `source_type` 缺『自报』」这项**已于 D18 结案**，见下方第 15 项。
>
> 15. **一条 WARN 混报了两类缺陷：自报分 `source_type` 补「自报」+ 裸 `T0` 可信度归位（D18）**：§26.5 查出「436 条自报分的
>    `source_type` 缺『自报』，抄写源是 `prompt.md` 的来源类型下拉表」，文档侧当天补了写法，本轮清存量。
>    第 22 轮四项拍板：**范围 = 修能修的 + 门禁放过空值**；原本带括号的 8 种写法（42 条）**原样挂后缀、容忍双括号**
>    （保变换统一 → 逆还原只需剔后缀，验收成本最低）；裸 `T0` 可信度一并改（**先按 31 条拍、实测发现是 122 条后停下来重报重问**，
>    改判「122 条全改」）。数据面写盘 **423 个条目 / 73 条记录**：`source_type` 改 **308 条**（R1 复用库里已有写法 156 /
>    R2 换尾括号 `（转述）`→`（自报分转述）` 7 / R3 新增变体 145；保真断言「原写法含转述 ⇔ 新写法含转述」实测 0 偏移，
>    **全程不删也不注入「转述」**）、`confidence` 改 **122 条**（`T0`→`T0-自报`；实测裸 `T0` 只存在于自报分段，
>    `independent` / `arena_elo` 各 0，且 122 条无一含转述 → 触发「转述来源不得配 T0/T0-自报」ERROR 的风险条数改前实测 0）。
>    门禁面：`self_reported` 那条「建议体现自报属性」原本对 `source_type` 为空也报，等于把**「主张了一个中立来源」（写错）
>    与「根本没主张来源类型」（缺失）混成一条 WARN** —— 混报时 436 既不能当「要改多少」也不能当「缺多少」，改完也无法验收
>    （改数据只减 308，减不到 436）。改为空值不报后，**WARN 逐级记账 469 → 341 → 33**，两级差额 −128 / −308
>    **各自恰为一个可独立测量的量**（空值条数 / 非空缺「自报」条数），这是本轮的负对照；常量段 452 → 16。
>    剩余 **33** 全部可解释：`knowledge_cutoff` 格式 9、参数量缺未披露声明 3、`pricing.source_type` 与价格值矛盾 3、
>    有定价缺 `source_url` 1、规则 6.2 主键撞车 17。ERROR 全程 0，记录 933→933、跑分条目 5642→5642、`score` 多重集相同、
>    未改动行 **860/933 逐字节相同**、叶子差异**越界路径 0**（白名单仅 `benchmarks.self_reported[i].source_type|confidence`）、
>    逆还原逐字节复原，md5 `5d2b4eef…50c` → `b5eaf743ebbe462613b3613e04168755`。
>    **本轮最该被读到的是「报数口径」这条教训**：我给用户报的「31 条裸 `T0`」是**在 436 条 WARN 组内**数的，
>    按缺陷类全库数真值是 **122 条** —— 另外 91 条 `source_type` 已写对、只有可信度漏改，**因为不触发任何门禁信号所以从没进入过我的视野**，
>    用户是在一个偏小 4 倍的数字上做的决定。同一轮里同类陷阱又应验两次：**128 vs 775**（来源类型缺失：门禁可见子集 vs 全库）、
>    **39 vs 438**（转述对撞：窄口径 vs 宽口径）。加固版规矩：**登记任何遗留清单时必须同时写下判据原文、该判据的命中数、
>    以及放宽判据后的命中数**；只写一个数等于给下一轮埋雷。另记**复核脚本自己也会错**：独立复核的逆还原报「与 HEAD 不逐字节相等」，
>    根因是回填用 `ia.get("source_type")` 无条件赋值，而 `dict.get()` 对不存在的键返回 `None`，凭空造出 `source_type: null`
>    改了键集与键序；**规律：用 `.get()` 做回填/复原时，缺键与值为 null 是两回事；凡逆还原报不等，先怀疑复核脚本，再怀疑数据**。
>    **遗留登记四类（本轮不修）**：① 可信度 `T0-自报-转述` 但来源类型写可直读官方文件 —— 窄口径 **39 条** / 宽口径 **438 条**
>    （本轮一条没减），且窄口径那 9 个串被本轮挂了后缀**已搜不到**，指南 §27 给出改后对应串与「不再唯一标识」的提醒；
>    ② `source_type` 为空且可信度在 T0 家族 = **128 条**，门禁放过后**对门禁完全不可见**，只能靠 §27 清单追踪；
>    ③ 自报分段 `source_type` 为空**合计 775 条**（**D18c 补注作用域**：仅自报分段；三段全扫 782 条），其余 **647 条连 `confidence` 键都不存在**；
>    ④ ~~**门禁判据本身有个洞没堵**~~ **→ D18b 已按拍板补上留痕（门禁规则 6.3），见第 16 项** —— 那条 WARN 是**纯子串匹配** `"自报" in source_type`，于是把可信度值当来源类型写
>    （`"source_type": "T0-自报"`）天然满足它、能过门禁却**零来源信息**，实测跑分三段共 **607 条 / 62 条记录**，
>    另有 ~~405~~ **406** 条把 `T0` 前缀缀在真实来源描述前（**D18b 复算更正**：宽判据「以 `T0`–`T4` 开头」合计 **1013**，1013 − 607 = 406）。**含义：这条 WARN 归零有一部分是空心的** —— 归零只说明「不再有裸写法」，
>    不等于「来源类型栏都写了来源类型」；升成语义判断要维护合法来源类型白名单（现库 191 种写法，**D18c 补注：191 = 仅自报分段，三段全扫 237 种**），成本未评估 → ~~存量待拍板~~ **已拍板「只加门禁留痕、数据一字不动」（见第 16 项）**，
>    新增已在 `prompt.md` 用显式禁令挡住。机制与全部教训见指南 **§27**。
>    **⚠️ 本报告正文里凡以 WARN 469 为基线的表述，自 D18 起过时**（~~现基线 **33 = 常量段 16 + 主键撞车 17**~~ ~~**D18b 起为 640 = 常量段 16 + 主键撞车 17 + 规则 6.3 的 607**~~ **D18c 起为 122 = 常量段 16 + 主键撞车 17 + 规则 6.3 剩的 89**）；
>    但历史行的 469 是对的，对应改前门禁与改前数据，不必回改。
> 16. **【D18b · 2026-09-02 同日】上一项 ④ 那条门禁盲区已按拍板补上留痕，数据一字未动。** 第 23 轮拍板
>    **「只加门禁留痕，数据一字不动」** → 门禁新增 WARN 级**规则 6.3**：跑分条目的 `source_type` **整栏就是一个可信度等级值**
>    （正则 `^T[0-4](-自报)?(-转述)?(-第三方)?$`）即报。**为什么用正则而不用 `CONFIDENCE_ENUM` 成员判定**：实测 `T2-第三方` 3 条、
>    `T3-转述` 1 条**在枚举外**，按枚举判会漏掉这 4 条（607 → 603）—— 枚举是「合法可信度值」的表，不是「长得像可信度值」的表。
>    **为什么三段都查**：`arena_elo` 段此前完全不读 `source_type`，该段那 3 条对旧门禁彻底不可见。
>    **为什么没选「一律置空」**：提请拍板前把 607 条按「同一条目有没有 `confidence` 键」切成三档 —— 甲 **123** 两栏一致（置空零损失）、
>    乙 **399** 连 `confidence` 键都不存在（`source_type` 是该条目唯一的等级线索，置空即销毁，涉 37 条记录）、
>    丙 **85** 两栏矛盾（84 × `T0-自报` vs `T0-自报-转述`、1 × `T0-自报-转述` vs `T3`；判哪个错要知道真实转述源 → 属重采）。
>    **一律置空会毁掉乙档 399 条的唯一线索**，所以停下来重报重问，没有按最初的想法动手。
>    **验收（全部实测）**：门禁 933 / ERROR **0** / WARN **33 → 640**（差额 **607** 恰等于独立脚本实测的规则 6.3 命中数；
>    分段 `self_reported` **600** / `independent` **4** / `arena_elo` **3** 逐项一致；值分布 `T0-自报` 379 / `T0-自报-转述` 200 /
>    `T0` 22 / `T2-第三方` 3 / `T3` 2 / `T3-转述` 1）、**数据 md5 仍为 `b5eaf743ebbe462613b3613e04168755`（零数据改动）**、
>    负对照「新门禁 × **改前备份** = WARN **948** = 341 + 607」证明这 607 条改前改后完全同数（D18 既没造出也没修掉）、
>    文档示例护栏 `temp/d17c_example_gate.py` 仍正对照 exit 0 / 负对照 exit 1、剩余 33 条与新规则命中**零重叠**。
>    **刻意不覆盖**冗余前缀类 **406 条**（如 `T0 官方一手技术报告（自报）`，信息不丢、只是前缀冗余）—— 一条 WARN 只背一类缺陷。
>    **升 ERROR 的条件**沿用规则 1.1/1.2：下一个采集周期新记录命中 0 且这 607 条已重采补全或按拍板处置。
>    **仍开着的遗留**：~~乙档 399 条要的是**迁移**不是置空（那 4 条枚举外写法逐字迁移会立刻造 ERROR，得先决定扩枚举还是改写成 `T2`/`T3`）~~
>    **→ ✅ D18c 已结案（见第 17 项）：迁移 395 条，枚举外那 4 条既没扩表也没改写，而是原样不动、单独登记（它们的等级本身有争议）**、
>    丙档 85 条属重采、406 条冗余前缀类无任何门禁信号、与上一项 ① 的宽口径 438 条**重叠 83 条**（同一条目背两类缺陷，修时要一次修完；
>    **D18c 复核：这 83 条全落在丙档，丙档一字未动 → 重叠一条没减**）。
>    **顺带更正上一项两处口径**：④ 的冗余前缀类 ~~405~~ → **406**；① 的窄口径 39 条**判据在改后库上重跑只得 0 条**
>    （改前快照重跑仍是 39 条 / 9 种、分布逐项吻合）—— 判据已被 D18 的写盘动作作废，那 39 条只能按「记录 + 数组下标」定位；
>    并已把宽口径 438 的**判据作用域补全**（`confidence == "T0-自报-转述"` 且 `source_type` 非空不含「转述」，**仅 `self_reported` 段**；
>    放宽作用域后为 440 / 458 / 460）—— 本轮复算正是漏了作用域才误判文档写错，而同一批复算里 405 那个数**确实是文档错**。
>    **两条教训**：说「某缺陷已清零」之前先问判据查的是**字段语义**还是**字符串表面属性**；复算文档数字先确认**判据作用域**。
>    机制、判据原文与全部验收见指南 **§27.1**。
>
> 17. **【D18c · 2026-09-02 同日续】把等级值迁进可信度栏 + 清甲档冗余 —— 518 个条目，WARN 640 → 122**
>    **第 24 轮拍板「乙档迁移 + 甲档清冗余」**。第 16 项登记「乙档 399 条要的是迁移不是置空」后单独提请裁定。
>    **为什么值得做**：乙档 399 条**根本没有 `confidence` 键**（涉 37 条记录），等级只写在来源类型栏里 →
>    §5「任何跑分结论不得仅基于 `T0-自报`」这类规则对它们**无从执行**，下游读不到可信度。迁移是把信息搬回它该在的栏目，不是清洗。
>    **三档身份本轮独立重算**，与第 16 项登记的 123 / 399 / 85 逐项吻合：甲档 = 可信度栏已有**同值** 123 条（全在自报分段）；
>    乙档 = **无可信度键** 399 条（自报分段 393 / 独立评测段 4 / 竞技场段 2）；丙档 = 两栏**矛盾** 85 条（自报分段 84 / 竞技场段 1）。
>    **处置**：乙档迁移 **395** 条（可信度栏 ← 原来源类型栏值，**逐字搬运不改写**，新键插在 `source_type` 紧后面；来源类型栏 ← `null`）；
>    甲档 **123** 条只把来源类型栏 ← `null`（可信度栏一字不动）；**丙档 85 条一字未动**（属重采）。
>    **枚举外 4 条原样不动**（`T2-第三方` 3 + `T3-转述` 1）：逐字迁移会立刻把 WARN 换成 **ERROR**（不在 `CONFIDENCE_ENUM` 内），
>    机械改写成 `T2`/`T3` 又等于替采集者做判断 —— 这 4 条的等级本身就有争议：
>    `cognition:swe-1-6:base` 自报分段[1] 写 `T3-转述`，但网址是头条、备注明写「**转述自 Cognition 官方博客**」→
>    按 §5.1 与决策 2 本该记 `T0-自报-转述`，记 T3 等于把**厂商的话**当成**媒体的话**；
>    `mistral:mistral-large-3:base` 独立评测段[5][6][7] 三条同址同标 `T2-第三方`，其中 **[5] 的备注**明写「**Artificial Analysis 评测**」而
>    §5.1 把 AA 列为 **T1 独立评测**的典型例子，网址又是 OpenRouter（第三方托管转述）→ 该记 T1 还是「T1 经转述」机械推不出来。
>    **为什么置 `null` 而不是删键**：`prompt.md` 明文「缺失一律填 null」；键留着 → 键序不变 → **逆还原只需按值回填、不必记插入位置**；
>    与 D17 的 `available: null`、D16 的五键 `free_tier` 同口径。**代价**：来源类型缺失从此有**两种形状**
>    （原本「无键」775 条 + 本轮新置「值为 null」515 条）→ 将来测缺失数必须两种都数，只数一种会少 515。
>    **验收（全部先在内存里跑完才落盘）**：前置断言现盘 md5 == 备份 == `b5eaf743…8755`、**序列化往返 933/933 逐字节相同**
>    （本库是 `ensure_ascii=False` + 默认分隔符，**不是压缩写法** —— 先验过才敢拿「未改动行逐字节」当 rail）、
>    记录 933→933、跑分条目 5642→5642、`score` 多重集相同、未改动行 **880/933 逐字节相同**（改动落在 53 条记录）、
>    叶子级 diff **越界路径 0**（白名单仅 `benchmarks.<段>[i].source_type|confidence`；差异 **913** 处 = 乙档 395×2 + 甲档 123×1）、
>    **逆还原与改前逐字节相等**（删 `confidence` 用 `del` 而非置 `null`，否则会留下原本没有的键）、乙档新增键越位 **0**、
>    迁移保真四项全 **0**（可信度值≠原值 / 未置 null / 多出别的键 / 其他键被改）、甲档「可信度栏被改动 **0**」、
>    **改后仍被规则 6.3 照出的 89 条 == 丙档 85 ∪ 枚举外 4**（按 记录+段+下标 逐条比身份）**且这 89 条一字未动**、
>    未参与的 5124 条被改动 **0**、`benchmarks` 以外差异记录 **0**、门禁 **933 / ERROR 0 / WARN 122** exit 0、
>    文档示例护栏 `temp/d17c_example_gate.py` 仍 pos **0** / neg **1**。
>    **WARN 记账（比总数更硬的是「其余各类逐条不变」）**：规则 6.3 **607 → 89（−518）**，规则 6.2 主键撞车 17→17、
>    `knowledge_cutoff` 9→9、参数量缺未披露声明 3→3、`pricing.source_type` 与价格值矛盾 3→3、有定价缺 `source_url` 1→1
>    —— **差额 518 全部落在规则 6.3 一类**，可执行等式 **640 − 122 = 518 = 改动条目数**。
>    另：`建议体现「自报」属性`那条 WARN 改前改后都是 **0** 命中（迁移后 22 条可信度变成裸 `T0`，但其来源类型栏已置空，
>    该判据有 `and stype` 前置 → 不报）。**这正是备选方案「只补可信度栏、来源类型栏留着」会多吃 22 条 WARN 的原因**
>    （640 → 662，且规则 6.3 一条不减），提请拍板时已量化。
>    **改后面的存量**：可信度栏有键 4540 → **4935**；来源类型缺失 775 → **1290**（自报分段）/ 782 → **1300**（三段）；
>    规则 6.3 命中 607 → **89**（自报分段 85 / 独立评测段 3 / 竞技场段 1）；非空写法 237 → **235 种**
>    （`T0` 与 `T3` 两种写法自此在库里消失 —— 它们的每一处出现都被本轮置空了）。
>    数据 md5 `b5eaf743ebbe462613b3613e04168755` → **`3c88eec4b25afe2e107e4fc5d1a84d77`**，
>    备份 `model_data_v2.jsonl.d18cbak-20260902-201552`（`.gitignore` 已忽略）。
>    **仍开着的遗留**：① 枚举外 **4 条**（上述两条争议，须回原始来源定夺）；② 丙档 **85 条**（两栏矛盾，属重采），
>    其中 **83 条**同时落在第 15 项 ① 的宽口径 438 重叠里，**一条没减**；③ ~~**406 条冗余前缀类**仍无任何门禁信号~~
>    **→ ✅ D18d 已结案（见第 18 项）：405 条剥掉前缀、1 条表外原样不动；但「无任何门禁信号」这半句没变 —— 剥完这类写法在库里归零了，~~门禁仍然不会报任何一次新写进来的前缀~~**
>    **→ 续 ✅ D18e 已结案（见第 19 项）：门禁补了 WARN 级规则 6.4 专照这类前缀，新写进来的会被报出来，「无任何门禁信号」这半句到此彻底失效**；
>    ④ 来源类型缺失涨到 **1290 / 1300**，门禁放过空值 → 对门禁完全不可见，只能靠清单追踪。
>    **两条教训**：① **把信息从 A 栏搬到 B 栏之前，先确认 B 栏的合法值表接得住 A 栏的写法** —— 接不住的那几条要单独拎出来，
>    不要为了让变换统一而改写它们（本轮若按「一律迁移」动手，那 4 条会把 WARN 换成 ERROR、门禁对全库直接失败）；
>    ② **打印语句与断言不一致时以断言为准，但那行打印必须当场修掉** —— 收尾核对脚本第一版打印「被改过的条目数 89（必须为 0）」
>    而紧随的断言通过，因为打印用了 `eo[k] is not en[k]`（对两个分别解析出的 dict 恒真）；断言才是取证件，打印只是给人看的，
>    但不修的话下一个人会照着那个 89 写文档。
>    机制、判据原文与全部验收见指南 **§27.2**。
> 18. ✅ **D18d 剥掉来源类型栏里当前缀写的等级值（405 个条目）**：第 25 轮拍板 **「全剥 406 + 补挂「自报」+ 6 条粘连的迁进可信度栏」**。
>    缺陷是 `"source_type": "T0 官方自报"` 这种**把可信度等级当前缀缀在真实来源描述前**的写法 —— 信息不丢、只是前缀冗余，
>    但它让这一栏同时背两种语义，也是第 16 项「规则 6.3 刻意不覆盖」留下的那批。
>    **口径按构造给出，不靠正则猜**：前缀类 = 以等级开头（`source_type` 非空 ∧ `^\s*T[0-4]`）**−** 纯等级值（规则 6.3 的正则）= **406**；
>    这与 D18b 在另一个快照上记的 `1013 − 607 = 406` 同数，反过来证明 D18c 一条没碰过这类。
>    前缀抽取用**长度降序的 25 种等级写法表做最长匹配**再吃掉分隔符（`SEP = " \t:：、,，)）-—"`），不用会回溯的正则量词 ——
>    第一版用 `T[0-4](-自报)?…[分隔符]*` 时 `T0-自报 官方一手` 会回溯成前缀 `T0` + 剩余 `-自报 官方一手`，
>    于是窄口径 495 与规则 6.3 的 89 **重叠**，违反「一条 WARN 只背一类缺陷」。
>    范围 **52 条记录 / 32 种写法**（自报分段 399 / 独立评测段 7）。
>    **本轮最该被读到的一个发现**：406 条里**没有一条是真正的两栏矛盾** —— 可信度栏同值 84、比前缀更具体 316、无键 6，
>    即前缀携带的等级信息在同一条目的 `confidence` 里**逐条都能读到** → 剥掉是零损失的（与第 17 项丙档 85 条属两类，别混）。
>    **三条变换规则**：① 剥前缀 + 吃分隔符；② 粘连写法 `T0-自报-技术报告` **5 条**走 D18c 的迁移（等级值搬进 `confidence`、
>    新键插在 `source_type` 紧后面，来源类型栏留下 `技术报告（自报）`）；③ 自报分段 ∧ 可信度在 T0 家族 ∧ 剥完不含「自报」→
>    末尾补挂「（自报）」，实测 **55 条**。**实写 405 条 / 51 条记录**（只改来源类型栏 400 + 同时改两栏 5）。
>    **表外 1 条原样不动**：`T1.5-第三方评测`（`deepseek:deepseekmoe-16b:base` 独立评测段[0]）—— `T1.5` 不在 `CONFIDENCE_ENUM` 内，
>    迁移会造 ERROR；改写成 `T1`/`T2` 等于替采集者做判断（与第 17 项那 4 条同理），故单独登记。
>    **提请拍板时量化了备选方案的副作用**：「只剥不补挂」会让 **55 条**丢掉自报标记 → 新触发「建议体现自报属性」WARN 55 条 →
>    **122 变 177**；本方案补挂后新增 **0**。（此前登记过的 66 是**另一个口径** —— 「剥完不含自报」而不管原值有没有；
>    真会撞门禁的是 **50** 条，按 13 种口径逐一试探后取 D == E == 50 交叉验证。）
>    **验收（全部先在内跑完才落盘）**：前置断言现盘 md5 == `git HEAD` blob == 备份 == `3c88eec4…d77`、序列化往返 933/933 逐字节、
>    记录 933→933、跑分条目 5642→5642、`score` 多重集相同、未改动行 **882/933 逐字节相同**、
>    叶子级 diff **410 处 / 越界路径 0**（白名单仅 `benchmarks.(self_reported|independent|arena_elo)[i].(source_type|confidence)`；
>    出现 3 个及以上差异叶子的条目 **0**）、**逆还原逐条通过 405/405**、
>    **可执行等式：以等级开头 495 → 90 = 纯等级值 89 + 表外 1，差额 405 == 本轮改动条目数**、
>    **形状三分 4342（非空）+ 782（无键）+ 518（值为 null）== 5642 改前改后各自成立**（本轮既不新造 null 也不删键）、
>    表外 `T1.5-第三方评测` **逐键相同**、门禁 **933 / ERROR 0 / WARN 122 → 122** exit 0、
>    文档示例护栏 `temp/d17c_example_gate.py` 仍 pos **0** / neg **1**、门禁脚本自身 md5 `f70bcb83…449e` 一字未动。
>    **信息零损失按可执行判据逐条论证（不是断言「应该没丢」）**：被剥掉的头部里那个等级值，改后必须仍能在**同一条目的 `confidence`** 里读到
>    （同值，或是它的更具体形式）→ 不满足的 **0** 条，关系分布**更具体 316 / 同值 89**。
>    **最强也最该如实说的一条证据**：改前改后两份门禁报告**除标题里的文件名外逐字节相同**（各 194 行、六类 WARN 数量逐项一致）
>    → 这轮改动**对门禁完全不可见**，价值是纯语义的，不能包装成质量指标提升；
>    **推论是现在没有任何机制阻止它回归**，只有 `prompt.md` 的禁令在守，而文档自己错过两代（指南 §26.5）。
>    **本轮建立的新保真不变量**：含「自报」的**条目数 2567 → 2567 一条不变**，而**写法种类 178 → 171** ——
>    两个数必须一起看，否则「归一重复写法」与「给条目加上/摘掉自报属性」在验收上无法区分。
>    **改后存量**：非空写法 235 → **228 种**、可信度栏有键 4935 → **4940**、前缀类命中 406 → **1**（只剩表外那条）、
>    规则 6.3 **89 → 89**（本轮一条不碰纯等级值）、来源类型缺失 **1300 → 1300**（无键 782 + 值为 null 518，两种形状都数了）。
>    数据 md5 `3c88eec4b25afe2e107e4fc5d1a84d77` → **`3553105b811c7104199d7de2b8052e91`**，
>    备份 `model_data_v2.jsonl.d18dbak-20260902-210735`（`.gitignore` 已忽略）。
>    **四条教训**：① **验收脚本自己出 bug 造成假警报已是第三次**（D18 是 `.get()` 凭空造键、D18c 是 `is not` 比两个 dict 恒真，
>    本轮是无条件剥尾部「（自报）」—— 那 20 条 `T0 官方一手技术报告（自报）` 的后缀是 **D18** 挂的、不是本轮挂的，
>    于是报「旧值不以新值主体结尾」）；修法是把逆还原写成两分支并**断言两分支不会同时成立**（歧义条目 **0**）——
>    若同时成立，头部就有两种拆法、整道 rail 失效，**所以「唯一性」本身要当断言写，不是修完就算**；
>    ② **报数前先确认判据的作用域，这已是第三次栽在同一处**：我按第 17 项「仅自报分段」的口径预测缺失形状 775 / 515，
>    而扫描器覆盖三段 → 真值 782 / 518；已补 `4342 + 782 + 518 == 5642` 当自洽检查；
>    ③ **用集合比，不用计数比**：写法探针里 `len(trig) == len(t0self)` 打印「两者相等」（50 == 50），但**集合不同**
>    （trig 里有 5 条前缀是 `T0-自报-转述`）—— 与第 17 项教训 ② 同一失败模式；
>    ④ **「WARN 没动」不等于「这轮什么都没做」**，但要拿得出证据：逐字节同构的两份报告 + 一条语义不变量（2567 → 2567）。
>    **仍开着的遗留**：① 表外 `T1.5-第三方评测` **1 条**（本轮新登记）；② **5 条 `（官方技术报告）（自报）`** 双括号观感遗留
>    （粘连写法迁移 + 补挂叠出来的，信息不错、只是难看）；③ ~~**前缀类无任何门禁规则** —— 是否补一条 WARN 级规则 6.4 属单独拍板项，
>    现库命中会是 **0**，纯防回归~~ **→ ✅ D18e 已结案（见第 19 项）：第 26 轮拍板补了 WARN 级规则 6.4，「现库命中会是 0」这个预测被实测证实（确为 0），
>    防回归不再只有 `prompt.md` 的禁令在守**；④ 这一栏**仍不收敛**：228 种写法里 D18d 新造 **24** 种，31 种剥离结果里只有 **7** 种是库里原有的。
>    **文档同日改**：`prompt.md` 的 178 种划掉换成 **171 种（D18d 后）**并新增「种类数会降、含自报的条目数不该动」的验收口径、
>    406 条那段划掉换成结案块 + **新增前瞻禁令「新采集不得把等级值当前缀写进 `source_type`」**（并写明目前没有任何门禁规则照这类）；
>    指南 §27.1 两处 406 的表述加删除线指向新增的 **§27.3**、抬头轮次范围 `D2–D18c` → `D2–D18d`；本报告抬头十七→**十八处**、
>    第 17 项遗留 ③ 划掉指向本项、§1 与 §6.2 的 WARN 演进行同步；`docs/交接_2026-09-01_D16结案.md` 横幅补 D18d；记忆两处同步。
>    机制、判据原文、变换对照表与全部验收见指南 **§27.3**。
>
> 19. ✅ **【D18e · 2026-09-02 同日再续】给前缀类补门禁留痕 = WARN 级规则 6.4，零数据改动。**
>    第 26 轮拍板 **「补，用『等级值 + 分隔符 + 主体』形」**，处置上一项遗留 ③：D18d 把存量剥净之后
>    **没有任何机制阻止新采集把它写回来**，挡它的只有 `prompt.md` 一句禁令 —— 而指南 §26.5 已实证文档自己错过两代、
>    且门禁只校验记录不校验文档。
>    **判据形是三选一，先把三个形都在现库与改前备份上量过再提请拍板**（不是先拍再量）：
>    A 形「以等级开头就算」（非空 ∧ `^\s*T[0-4]` ∧ ¬纯等级值）现库 **1** / 备份 **406**；
>    B 形「等级值 + 分隔符 + 主体」**未加前置**时现库 **89** / 备份 **494**，与规则 6.3 **重复计 89 条**；
>    **B′ = B + ¬纯等级值前置（选定）** 现库 **0** / 备份 **405**，与 6.3 **零重叠**；
>    C 形「只看开头」最宽，现库 **90** / 备份 **495**。
>    **四个形的差额都是 405，恰等于 D18d 的实写条目数** —— 这反过来给第 18 项又添一道独立负对照
>    （D18d 自己的等式是「以等级开头 495 → 90」，与这里的 A/C 两形互相印证）。
>    **选 B′ 不选 A 的理由**：A 形现库那 **1** 条命中就是第 18 项刻意不动的表外值 `T1.5-第三方评测`
>    （`deepseek:deepseekmoe-16b:base` 独立评测段[0]），它会把基线从 122 推到 **123**，
>    而且是**以「前缀冗余」的名义**去报一条真缺陷是「等级值不在 `CONFIDENCE_ENUM` 内」的条目 ——
>    与第 16 / 19 两项两次援引的「一条 WARN 只背一类缺陷」直接冲突。B′ 照不到它（`T1.5` 后接的是 `.` 不是分隔符）**是有意的**：
>    那条归「枚举外可信度值」那一项（与第 17 项表外 4 条同类，合计 **5 条**待单独处置）。
>    **三条实现要点**（都写在 `TIER_PREFIX_STYPE_RE` 上方注释里，不是只写在文档里）：
>    ① 调用处那道 `and not TIER_ONLY_STYPE_RE.match(stype)` 前置**是必需的、不是保险** ——
>    分隔符表含 `-`，少了它 `"T0-自报"` 会被解析成「`T0` + 分隔符 `-` + 主体 `自报`」而**同时**命中 6.3，
>    重复计 **89 条**（上表 B 形那一行就是这个 bug 的实测量）。两条规则各背一类：6.3 = 整栏就是一个等级值（零来源信息）；
>    6.4 = 等级值只是前缀（来源信息在、但一栏背两种语义）。
>    ② 分隔符字符类里 `-` **必须放最末**（`[ \t:：、,，)）—-]`）。写成 `)）-—` 会把 `）-—` 解析成 U+FF09→U+2014 的范围
>    而报 `bad character range`；**更糟的是若 ASCII `)` 排在前面，那是个合法的反向范围，会静默匹配约 8000 个 CJK 字符** ——
>    报错比沉默安全，这条在第 18 项教训 ② 已经栽过一次。
>    ③ **跑分三段都查**。`arena_elo` 段此前完全不读 `source_type`，D18b 补 6.3 时才纳入；6.4 同样要在该段查，
>    否则该段的前缀写法照旧不可见（改前备份实测本段命中 **0**，但判据不能靠这个省略 —— 下一批采集未必也是 0）。
>    **验收（零数据改动，所以 rails 全在门禁侧）**：**2×2 负对照矩阵，四个组合各自可解释** ——
>    旧门禁 × 现库 **122**、旧门禁 × 改前备份 **122**（印证第 18 项那句「两份报告除标题外逐字节相同」）、
>    新门禁 × 现库 **122**（规则 6.4 命中 0 → 基线一条不增）、新门禁 × 改前备份 **527 = 122 + 405**
>    （差额恰等于 D18d 实写条目数）；四组都是 933 条 / ERROR 0 / exit 0。
>    现库 WARN 构成逐项复核：规则 6.3 的 89 + 规则 6.2 的 17 + 常量段 16 + **规则 6.4 的 0** = **122**。
>    **本轮最该被读到的一道：五个探针的正对照。** 现库命中为 **0** 意味着「这条规则真的会响」在真库上**拿不到任何证据** ——
>    写错变量名、放错循环、正则不匹配，都会静默表现为「命中 0」，与「库里真干净」在门禁输出上**完全无法区分**。
>    所以必须自己造一条带前缀的记录喂门禁（载体 `alibaba:qwen2-1-5b:base`，全库 **95** 条合格载体之一；
>    三条挑选条件：原值非空且本身不触发 6.3/6.4、`confidence` **不在 T0 家族**否则「建议体现自报属性」会跟着 `source_type` 变而混淆计数、
>    整条记录无任何 6.3/6.4 命中）：**P0** 原样重写 → WARN **122**、且临时文件与真库 **md5 逐字节相同**
>    （这道前置是归因前提：若重写本身引入差异，P1–P4 的计数差里就混着序列化噪音，无从归因）；
>    **P1** `T0 官方自报` → **123**、只 6.4 响（证明规则接通了）；**P2** `T0-自报` → **123**、只 6.3 响（证明互斥）；
>    **P3** `T1.5-第三方评测` → **122**、两条都不响（证明「刻意不覆盖表外值」这条取舍真的生效）；
>    **P4** `官方技术报告（自报）` → **122**、两条都不响（证明本来就该这么写的值不误伤）。五个全 PASS。
>    另：**数据文件 md5 `3553105b811c7104199d7de2b8052e91` 一字未变**、`git status` 只有门禁脚本一个改动项；
>    文档示例护栏仍正对照 exit 0 / 负对照 exit 1（`prompt.md` 的 ```jsonl 示例里没有前缀写法，新规则不打它 —— 这本身也是一次确认）；
>    **门禁脚本 md5 `f70bcb836488860eb2708dd67625449e` → `6ea4de427c0875375b9fd83f6f0cf1c3`**（D18c / D18d 两轮都没动过它，
>    本轮是这三轮里唯一一次改门禁）。
>    **一条教训（本轮唯一，但已是第四次同类）**：**验收脚本自己的判据错，会报出与被验对象无关的假警。**
>    探针第一版五个里四个 FAIL，看着像「规则 6.4 没接通或互斥性破了」，实际门禁全对 ——
>    错在探针用「规则 6.3 的消息文本 in 整份报告」做**全报告范围**的子串匹配，而真库本来就有 **89 条**规则 6.3 的 WARN，这句恒真。
>    修法是**按 `## \`model_id\`` 标题精确截出载体自己那一段再搜**（注意 `alibaba:qwen2-1-5b:base` 与 `alibaba:qwen2-0-5b:base`
>    只差一个字符，子串匹配会串到隔壁段去），并补一道防呆：**期望至少一条要响时，载体段必须非空** ——
>    否则截段失败会静默返回空串，让 P0/P3/P4 假通过、P1/P2 假失败，诊断信息还指错方向。
>    这是第 15 项 `.get()` 凭空造键、第 17 项 `is not` 比两个 dict 恒真、第 18 项无条件剥后缀之后的**第四次**，
>    规律补一句：**判据的作用域要与被验对象的作用域一致 —— 验一条记录就在那条记录的段落里搜，别在整份报告里搜。**
>    **升 ERROR 的条件**沿用 §24 规则 1.1/1.2 与第 16 项规则 6.3（下一个采集周期新记录命中 0 且存量已处置 ——
>    现库已经是 0，所以这个条件从加上那一刻就成立一半），但**本轮刻意不升**，与 1.1/1.2/6.3 停在 WARN 的口径一致。
>    **仍开着的遗留**：① 表外 `T1.5-第三方评测` **1 条**（6.4 刻意照不到，与第 17 项表外 **4 条**同类，合计 **5 条**待单独处置）；
>    ② 丙档 **85 条**属重采；③ 来源类型缺失 **1300**（三段、两种形状）门禁放过、只能靠清单追踪；
>    ④ 该栏**仍不收敛**（228 种写法）；⑤ **5 条**双括号观感遗留。
>    **文档同日改**：`prompt.md` 一处 —— 把第 18 项写下的「目前没有任何门禁规则照这类，这条禁令只有本文档在守」划掉
>    换成 D18e 实况（规则 6.4 会报、现库命中 0、与 6.3 互斥、刻意照不到表外那条），指针扩到 **§27.4**；
>    指南 §27.3 遗留 ② 加删除线指向新增的 **§27.4**、抬头轮次范围 `D2–D18d` → `D2–D18e`；本报告抬头十八→**十九处**、
>    第 17 项遗留 ③ 与第 18 项遗留 ③ 划掉指向本项、§1 与 §6.2 的 WARN 演进行同步；
>    `docs/交接_2026-09-01_D16结案.md` 横幅补 D18e 与**门禁脚本 md5 变化**；记忆两处同步。
>    机制、判据三形对比表、探针表与全部验收见指南 **§27.4**。
>
> 另：正文 §6.2 与 §7 涉及架构字段的样例行（如 `aleph-alpha:pharia-1-llm-7b` 的 `Dense Transformer`）
> 是 D15 前的快照，**该写法现已越出稀疏性枚举**，主干部分归 `backbone_type`，不必再按旧样例理解。

---

## 0. 一句话结论

**数据层完好，但合并层此前存在系统性缺口：702 条花名册记录中 303 条（43.2%）从未合并采集成果，主库里躺的是 08-24 的 HF 骨架快照。本轮已补合并 277 条，缺口从 303 降到 26。**

采集工作本身是完成的（305/305 批次 submitted、702 个 model_id 全部存在），问题出在"合并"这一步没被验证——此前判定"0 缺失"用的是 `model_id 是否存在于主库`，而骨架早已预填，所以 `model_id 存在 ≠ 数据已合并`。

---

## 1. 全量校验结果

| 项 | 结果 |
|---|---|
| 主库记录 | **950 条**（花名册 702 + v1 遗留 248） |
| 门禁 ERROR | **0** ✅（验收标准 1 达标） |
| 门禁 WARN | 678（不阻塞） |
| schema | 全部 1.1 |
| 重复 model_id | 0 |

> **整改后现值（读表时替换使用）**：主库 **933 条**（花名册 685 + v1 遗留 248）、ERROR **0**、~~WARN **469**~~ ~~**WARN 33**~~ ~~**WARN 640**~~ **WARN 122**
> （D1–D10 期间为 689；D11 新增规则 6.2 照出 14 组待复测 → 703；D12 把来源名移出 `config` 后跨源并存显形，
> 规则 6.2 命中增至 **17 组** → 706；D13 废止两条「上下文须独立实测」类 WARN（命中 232）→ 501，
> 再把 27 条两栏填反的记录归位 → **474**；D14 移出 7 条自证非模型的记录（其自身贡献 5 条 WARN）→ **469**；
> D15 / D16 / D17c 三轮均为 **469** 不变；**D18 把「自报分 `source_type` 缺『自报』」那条 WARN 拆开并清存量 → 469 → 341（只改门禁放过空值，−128）→ 33（改数据补 308 条，−308）**，
> ~~现值 **33 = 常量段 16 + 规则 6.2 主键撞车 17**~~ ~~**D18b 起现值 640 = 常量段 16 + 规则 6.2 的 17 + 规则 6.3 的 607**~~
> **D18c 起现值 122 = 常量段 16 + 规则 6.2 的 17 + 规则 6.3 剩的 89**；
> **D18d 改了 405 个条目的数据（剥掉来源类型栏的等级值前缀）但 WARN 仍是 122 —— 改前改后两份门禁报告除标题外逐字节相同，数据 md5 → `3553105b811c7104199d7de2b8052e91`**；
> **D18e 反过来：数据一字未动、只给门禁加 WARN 级规则 6.4（照「等级值当前缀写进来源类型栏」），现库命中 0 → WARN 仍是 122 = 常量段 16 + 规则 6.2 的 17 + 规则 6.3 的 89 + 规则 6.4 的 0；
> 数据 md5 `3553105b…` 未变，门禁脚本 md5 `f70bcb836488860eb2708dd67625449e` → `6ea4de427c0875375b9fd83f6f0cf1c3`**）、
> 结构漂移 0；另有 10 条存疑记录原样存 `docs/unconfirmed_models.jsonl`、7 条非模型记录原样存
> `docs/non_model_records.jsonl`。见文首补记第 4、8、9、10、11、15、16、17、18、19 点。

### WARN 678 的构成（按类型）

| 数量 | 类型 | 性质 |
|---|---|---|
| ~~123~~ | ~~填了有效上下文但 notes 未注明独立测试方法与来源~~ | **D13 已废止该口径，检查项删除**（见文首第 10 点） |
| ~~105~~ | ~~有标称上下文但有效上下文为空，notes 未标「标称值，有效上下文待测」~~ | 同上（对应 §14 踩点 3，该行已改写） |
| ~220 | 自报跑分 `source_type` 未体现「自报」字样 | 规范性提醒，多为骨架残留条目 |
| 10 | 参数量全空但 notes 未声明「官方未披露」/「待补」 | 对应 §14 踩点 2 |
| 其余 | 散布的字段口径提醒 | — |

> ~~上下文窗口相关的两类合计 228 条，占 WARN 的 34%，是"notes 补写"这一项的主要工作量。~~
> **此判断已作废（D13）**：该口径经用户拍板废止，228 条不是欠账而是规矩与现实背离的症状；
> 现行检查只剩一条「有效上下文不得大于标称上下文」，当时命中 27 条并已全部归位。

### 验收标准逐条对照

| # | 验收标准（COLLECTION_PLAN_v2 §6） | 结论 |
|---|---|---|
| 1 | 全量 ERROR = 0 | ✅ 0 |
| 2 | 逐维度填充率较 v1 显著提升 | ✅ 见 §3（定位 / 多模态 / 自报跑分 / 上下文均提升） |
| 3 | `verification_status` 全部诚实 | ✅ 本轮修掉 3 条「已验证但无 verified_at」，现为 0 |
| 4 | `collected_at` 为真实采集日 | ✅ 分布见下，符合"不同模型不同日"的正确状态 |
| 5 | 占位符 notes 占比从 98.4% 降下来 | ✅ **5.6%**（53/950） |

`collected_at` 分布：`08-24: 82` / `08-25: 2` / `08-26: 193` / `08-27: 163` / `08-28: 359` / `08-29: 151`
（`08-24` 的 82 条 = 26 条待重采花名册 + 56 条无采集任务的 v1 遗留）

---

## 2. 🔴 重大发现：277 个采集文件从未合并进主库

### 2.1 怎么发现的

阶段 3 要求做"冲突裁决清单"，于是逐个比对 `incoming/models/*.jsonl`（采集产出）与主库同 `model_id` 记录：

- 319 个采集文件中，只有 **5 个**与主库完全一致，226 个有 21–50 处字段差异
- 按 `meta.collected_at` 判定方向：**279 个文件的采集日晚于主库，0 个早于主库**
  → 差异不是"主库后来更新了"，而是"采集成果根本没进去"

### 2.2 根因

主库 950 条中，702 条花名册模型的**骨架早已由 HF API 快照预填**（`WORKBUDDY_AGENT_GUIDE.md` §13）。
因此 `model_id 存在` 是一个**恒真**的判据——用它来验收"是否入库"，必然得出 0 缺失的假象。

真正的验收判据应为 `meta.collected_at` 是否已从骨架快照日（`2026-08-24`）推进到实际采集日。

### 2.3 处置

1. 生成待合并清单：`intermediate/merge_todo.txt`（277 个文件，全部预先跑过门禁 ERROR=0）
2. 用文档 §13 的既有策略一次性合并：

```bash
python scripts/model_data_tool.py merge \
  --file model_data_v2.jsonl \
  --incoming @intermediate/merge_todo.txt \
  --on-null take_source --on-both source_wins \
  --on-array replace --on-schema upgrade --tie-breaker keep_target --apply
```

3. 结果：**记录数仍 950（填骨架不加记录，符合预期），ERROR 仍 0，WARN 684 → 678**

| 指标 | 补合并前 | 补合并后 |
|---|---|---|
| 花名册中仍是骨架快照（`collected_at=08-24`） | **303** | **26** |
| 花名册已合并采集成果 | 399（56.8%） | **676（96.3%）** |

> 合并工具原本只支持 `--incoming` 单个文件，277 次调用意味着 277 次全库重写。
> 本轮给它加了**多文件 / 目录 / `@清单文件`** 三种传入方式（在内存中依次合并，最后只落盘一次、只备份一次），
> `@清单` 是专为规避 Windows 命令行长度上限（277 个路径约 16KB，超限）而加。

---

## 3. 填充率对比（花名册 685 vs v1 遗留 248）

统计脚本：`scripts/qa_stats.py`；填充判定 = 非 null / 非空串 / 非空数组 / 非全 null 对象。

> 口径已三次变更，下表为 **D13 上下文归位 + D14 移出 7 条非模型之后**的实测现值（`qa_stats.py` 于 933 条库重跑）：
> 分母 702 → 692（D6 隔离 10 条）→ **685**（D14 再移出 7 条自证非模型的记录），全库 **940 → 933**。
> **本节上一版表格停在 D8 时点、D13 之后未刷新**，本轮一并更正。最大的一处差值是 `有效上下文`
> 遗留组 **35.9% → 26.6%**：那是 D13 把 27 条两栏填反的记录有效栏置空的结果（27/248 ≈ 11 个百分点），
> **不是新的数据丢失**。`独立评测` 与 `Arena Elo` 两行含 D8 找回的 207 个跑分条目（见 banner 第 5 点）。

| 维度 | 花名册(685) | v1 遗留(248) | 差值 |
|---|---:|---:|---:|
| 发布日期 | 100.0% | 100.0% | 0.0 |
| **定位标签** | **90.7%** | 81.9% | **+8.8** |
| 开放权重 | 95.2% | 87.1% | +8.1 |
| API 可用 | 92.7% | 85.5% | +7.2 |
| 本地部署 | 89.5% | 75.4% | +14.1 |
| **总参数量** | **74.7%** | 56.0% | **+18.7** |
| 激活参数量 | 50.5% | 48.4% | +2.1 |
| **上下文窗口** | **68.3%** | 97.2% | −28.9 |
| 有效上下文 | 16.8% | 26.6% | −9.8 |
| 知识截止 | 16.8% | 25.4% | −8.6 |
| **多模态输入** | **83.8%** | 94.4% | −10.6 |
| 原生多模态 | 81.2% | 89.1% | −7.9 |
| **自报跑分** | **56.6%** | 66.1% | −9.5 |
| 独立评测 | **28.6%** | 49.2% | −20.6 |
| Arena Elo | 7.9% | 47.6% | −39.7 |
| 定价-输入 | 21.6% | 59.3% | −37.7 |
| 定价-输出 | 19.7% | 58.5% | −38.8 |
| 定价-生效日 | 100.0% | 96.8% | +3.2 |
| 来源 URL | 99.7% | 99.6% | +0.1 |

### 3.1 补合并带来的提升（同口径对比）

| 维度 | 补合并前 | 补合并后（D8 回补后现值） |
|---|---:|---:|
| 定位标签 | 57.5% | **90.3%** |
| 上下文窗口 | 44.6% | **67.6%** |
| 自报跑分 | 34.3% | **56.5%** |
| 多模态输入 | 52.4% | **83.5%** |
| 原生多模态 | 48.4% | **80.6%** |

> `独立评测` / `Arena Elo` 未列入本表：它们同时受「补合并提升」与「replace 破坏」两股反向作用影响，
> 拆分前的补合并前基线已不可单独归因，只在 §3 给出终值。

### 3.2 为什么部分维度花名册反而低于 v1 遗留（口径说明）

**这不是 v2 采集质量差，是样本构成不同：**

1. **v1 遗留的 248 条是早年投入充分的主流模型**（Qwen 全系、GPT、Claude 等），数据天然齐全；
   而 v2 花名册 702 条里 **406 条是开源权重模型**、含大量长尾/学术模型（如 `brain2qwerty`、`rnnsearch-50`、
   `nplm`、`digivio`、Bengio 2003 NPLM），这些模型公开信息本就稀少。

2. **定价必须按分型看**，但分型依据不是「是否开源权重」。开源与否不决定有没有官方价——
   DeepSeek / 阿里 Qwen / Moonshot Kimi / 智谱 GLM / MiniMax / Mistral / Cohere 都是**既开源权重、又公布自有官方 API 刊例价**，
   这类模型的定价本该照采。判定基准见 `multi_platform_subagent_guide.md` §5 红线 5（2026-08-29 由「是否开源权重」修订）。
   混在一起统计仍会严重低估真实覆盖：

   | 分组 | 定价填充率 |
   |---|---:|
   | 花名册 / 商业 API（191 条） | **50.3%** |
   | 花名册 / 开源权重（406 条） | 12.6%（**应接近 0，属正常**） |
   | 花名册 / 未知或混合（105 条） | 7.6% |
   | v1 遗留 / 商业 API（83 条） | 86.7% |
   | v1 遗留 / 开源权重（131 条） | 32.8% |

   → 真实的同比口径是 **50.3% vs 86.7%**（同为商业 API 模型），差距仍在，
   主因是花名册里的商业 API 模型有大量长尾厂商（如区域小厂），定价页难找或不存在。

   > **【本报告初稿结论已更正】** 初稿依旧红线 5 把这 32.8%（43 条）判为「疑似违规、建议核查后置 null」。
   > 逐条复核来源后该结论**不成立**：43 条中 **40 条 `source_type` 即厂商自家官方定价页/发布页且标 T0**
   > （含 14 条「已下架」模型的历史官方价），另 3 条为如实标注的媒体转述 T3，
   > **没有一条是拿第三方托管商（OpenRouter / 云厂商）报价冒充官方价的**。
   > 若照初稿建议执行，将直接删掉 40 条真实的官方刊例价——是口径错误导致的破坏性「整改」。

3. **Arena Elo 7.8% 属正常**：只有上过 LMArena 榜的模型才有该字段，长尾模型本就没有。

4. > **【重要更正 · 本文 §3 / §3.1 的 independent 与 arena_elo 两行是「破坏后」快照，现已回补】**
   > 本轮自己的「补合并 277 条」用了 `--on-array replace`，而采集分片普遍只填 `self_reported`、
   > `independent` / `arena_elo` 留空 `[]` —— **空数组把目标里已有的条目整组抹掉了**：
   > 按主键比对 **81 条记录 / 215 个条目**（`independent` 177、`arena_elo` 29、`self_reported` 9），
   > 丢的是前几轮人工采集的真数据（如 `cohere:cohere-command-a` 的三条 Arena Elo）。
   > 反常证据：从未被合并触碰的 08-24 骨架记录 `independent` 覆盖 **72%**（59/82），
   > 而被合并过的记录只有 **22%**（191/858）——同源于 Epoch/公开榜单的数据不可能差三倍。
   > 所以这两个维度的「花名册低于 v1 遗留」**不是样本构成差异，是本轮合并造成的破坏**；
   > 取证与恢复方法见 `WORKBUDDY_AGENT_GUIDE.md` §18。
   > 教训：**「记录数不变 + ERROR 0」这套验收口径看不见字段级破坏**，且合并后 WARN 由 684 降到 678
   > 当时被当成成绩记录——那 6 条正是被删掉的跑分带走的 WARN。
   >
   > **【D8 处置结果】** 已按主键「只回补不覆盖」找回，累计净回补 **82 条记录 / 207 个条目**：
   > 其中 `self_reported` 的 9 条经逐条核对**不算损失**（`nvidia:llama-nemotron-ultra-253b:base` 的旧
   > `name`/`mode` 写法被后来的 canonical 写法取代，属合法 schema 升级，已撤回），故不计入。
   > 另有 1 条 `independent` 早在恢复基线（943b6f2）之前就已丢失、按单一基线扫不出来，
   > 回补后复跑全历史取证才发现并一并找回 —— 这就是「回补完必须用全历史最大长度复扫」的理由。
   > 回补后 `independent>0` 全库 22% → **33.8%**。
   > **新开待拍板项**：主键 `(benchmark, config, date)` 认不出 legacy `name` 写法，回补后 207 条里有
   > 2 条与既有条目同名同分、3 条同名不同分（例如 `openai:gpt-5-2-2025-12-11-xhigh:base` 的
   > GPQA Diamond 0.914 同时存在 T1 直连与 T3 转述两条）。两条都带完整来源，未自动删。
   > 其根因（非 canonical 写法条目主键认不出）**已由 D9 + D10 全部消除**（`name` 1293 条 + 其余三种写法 57 条，
   > 现库缺 canonical 主键 0），见 banner 第 7 点；
   > 归一后全库精确主键重复为 **22 个数组 / 39 组**（D9 改动记录内 6 个）。「同名基准分数冲突取哪一条」单独拍板
   > —— **已由同日 D11 结案**：实际构成是主键粒度不足而非来源打架，25 组机械修可分、14 组留作待复测，见 §6.2 与指南 §20。
   > 反方向的失效也存在：上述 GPQA Diamond 两条如今靠 `config`（一条 `null`、一条 `"xhigh"`）错开，
   > 精确主键**不再报重复**——`config` 留空与填值不等价时，去重会漏；实测全库另有 **7 组** 同基准同分、
   > 只因 `config` 写了不同来源名而并存（如 `alibaba:qwen2-5-max` 的 GPQA Diamond 0.587 ×2，
   > config 分别是 `default（benched.ai）` 与 `default（llmbase）`）。三类口径都可用 `temp/d9_residual_scan.py` 复跑。

### 3.3 占位符 notes 的口径修正

`COLLECTION_PLAN_v2` §6-5 原文口径是「待补/未含」。但若把"官方未披露""待实测"也算作占位符，
会把**真实采集结论**（查过了，官方确实没公布）误判成烂尾记录——长尾模型参数量普遍未公开，必然被误伤。

本轮把口径收窄为「待补 / 未含 / 需另行采集 / 待采集 / 未采集 / 待核实」，
并单独统计「确认官方未披露」类：

| 口径 | 数量 | 占比 |
|---|---:|---:|
| 真占位符 notes（未采到） | 53 | **5.6%** |
| 确认官方未披露（有效采集结论） | 243 | — |

---

## 4. 冲突裁决清单

### 4.1 结论：无需要裁决的冲突

本轮合并策略为 `--on-both source_wins --on-null take_source --on-array replace`
（依据 `WORKBUDDY_AGENT_GUIDE.md` §13：主库是预填骨架，合并是"填骨架"不是"加记录"）。
因此**没有产生 `on_both=conflict` 条目**，冲突裁决清单为空。

按 `prompt.md` 5.4 的可信度规则，实际生效的是一条**统一的优先级裁决**：
**人工实采值 > HF API 骨架快照值**。理由：骨架来自自动化快照推导，实采经过官方源直验。

### 4.2 合并前后的实质差异（等价于"被裁决掉的冲突"）

`scripts/diff_incoming_db.py` 的扫描结果（补合并前）：

- 采集文件与主库存在值差异的字段 **7804 处**
- 差异最多的字段：`pricing.source_type`(289)、`architecture.notes`(279)、`pricing.notes`(279)、
  `modality.*.notes`(各 278)、`basic_info.access.notes`(258)、`basic_info.release_date`(253)
- 骨架独有、合并时被保留的字段：`pricing.promotions`(103)、`pricing.long_context`(89)、`pricing.free_tier`(72)

典型裁决案例（骨架值 → 实采值）：

| model_id | 字段 | 骨架值 | 实采值（胜出） |
|---|---|---|---|
| `aleph-alpha:pharia-1-llm-7b:base` | `basic_info.full_name` | `Pharia-1-LLM-7B` | `Aleph Alpha Pharia-1 LLM 7B` |
| `aleph-alpha:pharia-1-llm-7b:base` | `architecture.architecture_type` | `Unknown` | `Dense Transformer` |
| `aleph-alpha:pharia-1-llm-7b:base` | `architecture.context_window_tokens` | null | 8192 |
| `google:datagemma:base` | `meta.notes` | Epoch AI 数据集转述 | 实采 T0 说明（arXiv 2409.13741） |
| `fugaku-llm`（§13 已记录） | `access.api` | `true`（HF 快照推导） | 实查无官方 API |

### 4.3 越界 positioning 的静默丢弃（已知未修项）

合并工具对**受控词表外的 positioning 标签**是静默丢弃的，
导致源文件合规但主库落成 `[]`，而主库校验仍是 ERROR 0。
`workbuddy-03` 已修源文件（`b168w1`/`b258w1`/`b259w1`），主库未回填，留给本阶段。

本轮补合并后花名册定位填充率已达 **89.5%**，剩余空 `positioning` 多为"确无适用标签"的历史模型
（如 Bengio 2003 NPLM，六个标签无一适用，记 `[]` 并说明是正确做法）。

---

## 5. 已知风险声明（分析端须显著标注）

1. **官方域不可访问属环境限制**。`docs.claude.com` / `platform.openai.com` 等官方域直连不可达，
   大量记录只能取 WebSearch 摘要，按红线降级标 `T3` / `T0-自报-转述`。
   **相当比例的记录可信度低于严格 T0/T1**——全库 `confidence` 分布：
   `T0-自报` 1546、`T1` 1145、`null` 1111、`T0-自报-转述` 799、`pricing:T0` 783、`T3` 725、`T0` 122、`T2` 13。

2. **Arena 数据来自镜像源**。原始 `arena.ai` 被 Cloudflare 拦截，
   442 条记录的 `source_type` 标注为「LMArena 镜像(DataLearner)」，非一手来源。

3. **定价需定期复核**。`pricing.effective_date` 有 854/942 条精确到日；
   **定价采集 30 天后应复核**（本轮主要集中在 2026-08-27 ~ 08-29）。

4. **社区异常信号不入规范基准**。某模型特定榜单骤降等信号，仅在 `notes` 保留风险提示，不进 `benchmarks`。

5. **骨架残留字段未经实采复核**。主库保留的 `pricing.promotions` / `long_context` / `free_tier`
   等骨架独有字段（共约 264 处）来自 HF 快照，未经本轮实采验证。

6. **~~v1 遗留记录的开源权重定价可疑~~ → 已更正，判定不成立（见 §3.2 更正框）**。
   131 条中 43 条有定价，初判「与红线 5 冲突、疑似第三方云转售价」；逐条复核来源后 40 条出自厂商自家官方定价页（T0）、
   3 条为如实标注的媒体转述（T3），**无一例转售冒充**。红线 5 的判定基准已于同日改为「厂商有无自有官方 API 刊例价」。
   → 这轮回扫真正查出的缺陷是另一类，且此前门禁完全不查：**`pricing.source_type` 声称查无官方价、价格键却仍挂着值**。
   全库 7 条，其中 4 条已按记录自身证据整改（D5：3 条剔 null + `aya-expanse-32b` 更正标签），
   余 3 条（`meta:muse-spark-1-1` / `muse-spark-1-2` / `microsoft:mai-code-1-flash`）价格取自头条号 UGC、
   需外部核实方能定方向，**已保留为 WARN 并登记待拍板，未靠猜测改动数据**。

---

## 6. 剩余待办

### 6.1 26 个花名册模型未合并（需重采）

补合并后仍有 26 条花名册记录停留在骨架快照日，**全部是"磁盘无采集文件"**——
对应早期 pilot 批次（OpenAI / Anthropic / Google / Meta / Mistral），文件在合并后未保留。

```
anthropic:claude-opus-4-1-20250805-16k / -27k
anthropic:claude-opus-4-20250514-16k / -27k
anthropic:claude-opus-4-5-20251101-16k
apple:openelm-270m
google:gemini-3-6-flash-minimal / gemini-embedding / gemma-4-31b-it-minimal / glam
meta:meta-llama-3-70b-instruct / meta-llama-3-8b-instruct
meta:muse-spark / muse-spark-1-1 / muse-spark-1-2
mistral:mistral-large:2411 / mistral-medium:2505 / mistral-moderation
mistral:mistral-nemo-base:2407 / mistral-saba
openai:babbage / babbage-002 / chatgpt-agent / code-davinci-002 / gpt-3-5-turbo:1106
technology-innovation-institute:falcon-arabic
```

重采后按 §2.3 的合并命令入库即可（清单可复用 `intermediate/merge_todo.txt` 的形式）。

### 6.2 其他

| 项 | 说明 | 建议 |
|---|---|---|
| WARN 678 → **689**（不含规则 6.2 的固定基线）→ 706 → 474 → ~~现值 **469**~~ → **33**（D18）→ ~~**640**~~ → **122**（D18c 改数据清掉 518）→ **122**（D18e 加规则 6.4、现库命中 0）| 689 的实测构成：自报分 `source_type` 未含「自报」440、有效上下文缺测试方法说明 127、标称上下文缺「待测」标注 105、`knowledge_cutoff` 格式 9、参数量缺未披露声明 4、`source_type` 与价格值矛盾 3、缺 `source_url` 1。**这 689 条从 D9 到 D12 全程一字未动**，用作负对照的常量段；D11 加规则 6.2（14 组）→ 703、D12 拆来源后 17 组 → 706、**D13 废止其中两条上下文 WARN（127 + 105）后常量段降到 457** = 474、**D14 移出 7 条非模型再减 5（自报分 4 + 参数量声明 1）→ 常量段 452**，加规则 6.2 的 17 组 = **469**；**D15 / D16 / D17c 三轮 469 未变**；**D18 把「自报分 `source_type` 缺『自报』」这一条 WARN 拆成两类缺陷后清存量 —— 门禁改为放过空值 → 341（−128，恰为空值条数），数据补 308 条「自报」→ 33（−308，恰为非空缺「自报」条数），常量段 452 → 16**；**D18b 只加门禁留痕、数据一字未动：规则 6.3 命中 607 → 33 + 607 = 640，数据 md5 `b5eaf743…` 未变**；**D18c 改数据 518 个条目（乙档 395 迁进可信度栏 + 甲档 123 清冗余）→ 规则 6.3 命中 607 → 89，640 − 518 = 122，且差额全部落在规则 6.3 一类、其余五类逐条不变；数据 md5 → `3c88eec4b25afe2e107e4fc5d1a84d77`**；**D18d 改数据 405 个条目（剥掉来源类型栏里当前缀写的等级值）→ WARN 122 → 122，账上差额 0 —— 不是「没做」，是这类缺陷本来就没有任何门禁规则照它（规则 6.3 只管「整栏就是一个等级值」），改前改后两份报告除标题外逐字节相同；数据 md5 → `3553105b811c7104199d7de2b8052e91`，验收要靠语义不变量（含「自报」条目数 2567 → 2567、写法种类 178 → 171）而不是 WARN 数**；**D18e 反过来 —— 数据一字未动、只改门禁：新增 WARN 级规则 6.4 照「等级值当前缀写进来源类型栏」这类（判据 = 非空 ∧ 以 `T0`–`T4` 等级值开头 ∧ 后有分隔符和非空主体 ∧ **整栏不是纯等级值**，最后那道前置让它与规则 6.3 天然互斥）→ 现库命中 **0**，WARN 122 → 122，账上差额仍是 0；负对照 = 改前备份命中 **405**（新门禁 × 备份 = **527 = 122 + 405**，差额恰等于 D18d 的实写条目数）。**D18d 与 D18e 两轮 WARN 都停在 122 但原因相反**：前者是「改了数据而门禁照不到」（数据 md5 变、门禁 md5 不变），后者是「改了门禁而现库本来就干净」（数据 md5 `3553105b…` 不变、门禁脚本 md5 `f70bcb83…` → `6ea4de42…`）—— 读的人要靠**哪个 md5 动了**分辨，别把「WARN 没动」一律当成「这轮什么都没做」** | ~~剩余最大项是 436 条自报分 `source_type`，属 notes 补写轮性质的采集工作~~ **→ D18 已结案：308 条挂后缀归一、128 条空值由门禁放过并登记待重采，见第 15 项**；其余为新增检查照出的存量（`knowledge_cutoff` 格式 9、参数量缺未披露声明 3、`pricing.source_type` 与价格值矛盾 3、有定价缺 `source_url` 1）。规则 6.2 那 17 条不是脏数据，是待复测清单本身。**注意 D18 也留下一条门禁盲区：那条 WARN 是纯子串匹配，`"source_type": "T0-自报"` 这种把可信度值写进来源类型栏的也能过（实测 607 条），所以归零是「不再有裸写法」而非「都写了来源类型」** **→ D18b 已加规则 6.3 把这 607 条照出来（WARN 级留痕、数据一字不动），见第 16 项；D18c 又把其中 518 个条目真正改了（等级值迁进可信度栏、来源类型栏置空），规则 6.3 现剩 89 条 = 丙档矛盾 85 + 枚举外 4，见第 17 项；D18d 再把规则 6.3 刻意不覆盖的那 406 条冗余前缀类剥掉 405 条（1 条表外 `T1.5-第三方评测` 原样登记），WARN 一条没动，见第 18 项；D18e 给这类前缀补上 WARN 级**规则 6.4**（与 6.3 互斥、现库命中 **0**、纯防新采集回归，负对照 = 改前备份命中 405），见第 19 项** |
| ✅ **上下文「须独立实测」口径已废止（D13）** | 旧口径要求 `context_window_effective_tokens` 未经独立测试（T1）一律填 `null`，但全库 208 条填值里只有 8 条符合设想、**173 条是标称值原样抄写**、27 条两栏填反，门禁两条检查为此沉默地挂着 232 条警告（占当时 WARN 的 33%） | 用户 2026-08-30 拍板**废止口径、173 条原样保留**；四份文档同步改写（执行细则 §2 / prompt.md 字段字典 / agent_prompt_per_model / 指南 §14），门禁两条旧 WARN 换成一条硬不变量「有效栏不得大于标称栏」；27 条填反的按「大值归标称栏、有效栏置空」归位，**`deepseek:deepseek-v3:0324` 例外**（其大值 163,840 是 T3 三方转述、不可覆盖官方 T0 的 131,072，故只清有效栏）。详见指南 §22 |
| v1 遗留开源权重定价 | ~~131 条中 43 条有定价，疑似违规~~ **初判不成立，已撤销**：40 条本就有厂商官方刊例价 | **不得按旧红线 5 置 null**，见 §3.2 更正框 |
| 定价矛盾待核实 3 条 | `muse-spark-1-1` / `muse-spark-1-2` / `mai-code-1-flash`：标签称无官方价、值却挂着 UGC 转述价 | 需回厂商官方价目页逐条核实后定方向（属重采范畴，待拍板） |
| ~~采集人标 `存疑` 的 10 条~~ **已处置（D6）** | `meta.verification_status == "存疑"` 逐条复核均查无立得住的依据，经用户拍板**全部移出主库**（950 → 940），原样存 `docs/unconfirmed_models.jsonl`，4 个对应采集文件移入 `incoming/models/_quarantine/` | 已结。这 10 个 model_id 不再计入花名册完成率（692/702 + 隔离 10），**不得当「漏采」重派**；重新入库须先拿到官方证据，流程见 `WORKBUDDY_AGENT_GUIDE.md` §17.5 |
| ✅ **主库范围收窄：只记录模型（D14）** | 库里混着 7 条**对象不是模型**的记录（训练系统、agent 系统、开放式算法框架、分布式训练技术）。宽松关键词不可用：按「框架/系统/平台/pipeline」扫命中 **79 条**，逐条读下来几乎全是误伤（「训练框架 hai-llm」是真模型的训练工具、「非模型上下文上限」是上下文口径说明、「专为代理式 AI 多智能体系统设计」是用途） | 改用**只认记录自己的声明**这一窄判据（`architecture_type` 值或 `notes` 里明说对象是系统/框架/非模型），命中 7 条全部人工核对原文后移出主库（940 → **933**、条目 5655 → 5642、WARN 474 → **469**），逐字节存 `docs/non_model_records.jsonl`、5 个采集文件移入 `incoming/models/_out_of_scope/`；验收 `md5(排序(主库+归档)) == md5(排序(改前备份))`。**乙档 3 条经用户点定保留**（`xai:grok-4-heavy` / `inspur:haiyue` / `huawei:pangu-5-0`）。**局限如实登记**：窄判据只抓得到「自己声明不是模型」的记录，采集者没写这句的系统级条目扫不出来。这 7 个 model_id 同样**不得当「漏采」重派**，花名册口径见指南 §23 |
| ~~`pricing.currency` 口径未定~~ **已拍板并归一（D7）** | 六价键全 null 的 645 条里 USD 323 / null 318 / 连 unit 也 null 4，接近对半。**根因是 `prompt.md` 字段说明原文写「`currency` 默认 `"USD"`」**——照文档写就会产出「有币种无价格」的记录 | 已按「无价即无币种」把 323 条归一为 null，门禁新增规则 4.3 防回归，采集侧三处文档同日改正。`unit` 保持 `per_million_tokens` 不动（量纲声明，不携带「已核实」语义） |
| ✅ **本轮补合并造成的跑分条目丢失（D8 已回补结案）** | `85b9fae` 用 `--on-array replace` 把 81 条记录 / 215 个**已采集**跑分条目覆盖成空（independent 177 / arena_elo 29 / self_reported 9），丢的是前几轮人工成果而非骨架填充值 | 见 §3.2 第 4 点与指南 §18。工具已加空数组保护（默认不覆盖，需 `--allow-empty-replace` 显式放行）并经 `temp/d8_verify_empty_array_guard.py` 验证；恢复脚本 `temp/d8_restore_benchmarks.py --apply` 已执行，再由 `temp/d8_fix_over_restore.py` 撤回 9 条属合法升级的 legacy `self_reported`、`temp/d8_restore_eurus_independent.py` 找回 1 条早于恢复基线丢失的条目，**累计净回补 82 条 / 207 个条目**，复检 940 条 ERROR 0。遗留项转为「同名基准冲突与 1293 条 legacy `name` 条目归一化口径」（后者已由 D9 + D10 全清，见下两行；全库清单改用 `temp/d9_residual_scan.py`） |
| ✅ **`benchmarks` 条目四种非 canonical 写法（D9 + D10 已全清）** | legacy 只有 `name` 的条目 1293 条（self_reported 1241 / independent 50 / arena_elo 2，涉及 156 条记录），另有 `benchmark_name` 30 / `metric_name` 23 / `arena_elo` 误用 `benchmark` 4（57 条 / 11 记录）。合并去重主键对这类行永远算出 `("None","None")`，同一次测量会静默并存两份 —— 不是格式问题，是**去重失效** | 全部改名为 canonical 主键（前两个数组 `benchmark`、`arena_elo` 用 `sub_benchmark`）；`temp/d9_normalize_benchmark_keys.py` 与 `temp/d10_normalize_benchmark_keys2.py` 都以「反向改名必须与原文逐字节相等」为验收，写盘后再对 `git show HEAD` 复核全部可还原、值零改动；现库**缺 canonical 主键 0**。规则 6.1 按「先清数据、再收紧检查」在 D10 后扩成「缺 canonical 主键即报」，负对照用改前备份实测 **1982 = 689+1293**（D9 时窄口径）、**746 = 689+57**（D10 前）、**2039 = 689+1293+57**（D9 前 + 全写法版）。详见指南 §19 |
| ~~剩余 3 种非 canonical 写法未归一（57 条 / 11 记录）~~ **已处置（D10）** | D9 按 `"name" in item` 圈范围，漏掉 `benchmark_name` 30 条（8 记录）、`metric_name` 23 条（2 记录）、`arena_elo` 误用 `benchmark` 4 条（1 记录）；**当时的规则 6.1 只看 `name`，对这 57 条完全沉默** | 已按 D9 同法归一（另加「同时锁记录数 11 与条目数 57」「主键重复数组数改前改后对照 22 → 22」两道保险）。教训：**归一化脚本的匹配条件就是它的盲区**，残留数必须独立跑全库。剩 8 条同时带 `benchmark` 与 `name` 的冗余条目（6 条同值、2 条不同值）canonical 主键已在、不影响去重，**刻意未动** |
| ✅ **同名基准主键撞车 39 组（D11 已结案）** | 先逐条摊开量化再定口径：38/39 组内 `confidence` 全同 → 「取高 confidence」只能裁决 1 组；35 组同名不同分里 34 组整组只有一个 `source_url` → **主要毛病是主键不足以标识一次测量，不是两个来源打架**（极端例：`sber:fred-t5-xl` 一条 RSG 挂 10 个子任务，子任务名只在 `notes` 里） | 按「notes 能否复原区别」分五档：A 15 组把 notes 写明的评测设置填进 `config`、B 5 组把 `score_type` 口径追加进 `config`、C 1 组把子任务拆进 `benchmark` 名、D 4 组纯重复删后到的一条、E 14 组不动（见下一行）。`temp/d11_resolve_benchmark_conflicts.py` 计划表逐条带 `(期望基准名, 期望分数)` 自校验位置，验收 = 反向还原与 `git HEAD` 逐字节相等 + 全库 `score` 零改动 + 条目 5659→5655。**顺带第一次写下 `config` 的语义边界**（指南 §20） |
| 🔴 **主键撞车待复测 17 组（D11 E 档 14 + D12 新增 3，属重采范畴）** | E 档 14 组：`megatron-turing-nlg-530b` 9 组（每基准 3 个分数、notes 只有公共描述，分不清哪列是哪个 shot）、`exaone-deep-2-4b` 2 组、`multi-token-prediction-7b` 2 组、`aya-expanse-32b` 1 组（一条 notes 未写对手）。**D12 又照出 3 组**（均在 `anthropic:claude-opus-4-5:20251101`）：同一次 Artificial Analysis 测量被两个镜像站各抄一次 —— GPQA Diamond 0.81/0.87、MMLU-Pro 0.889/0.9 是**分数分歧**，SWE-bench Verified 0.809/0.809 是**两镜像读值一致的真重复**。拆来源前它们被同一个 `config` 混在一起，拆开后才是真撞车 | **不猜**。由门禁规则 6.2（WARN）自动维护这份清单，现库 17 条命中即全部；要收口只能回原表重读。清单可复跑 `temp/d9_residual_scan.py` |
| ✅ **`config` 写来源名 92 条 / 21 记录（D12 + D12b 已结案）** | ~~7 组近似重复：同基准同分、只因 `config` 一个写 `default（benched.ai）` 一个写 `default（llmbase）` 而并存~~ 本轮独立复扫发现范围被严重低估：**92 条 / 21 记录**，剥来源名后撞键的组有 33 个（仅 6 组同分、**27 组分数有差**）。那 27 组**不是冲突，是不同第三方站各测一次**——正是 `independent` 数组存在的意义 | 方向与「剥掉来源名再合并」**相反**：按用户拍板选 A 方案，`independent` **新增 `source_site` 字段并入合并主键**（`benchmark, config, source_site, date`），来源身份从 `config` 迁进 `source_site`，`config` 回归纯评测配置。净改动 145 条 / 23 记录、`score` 零改动、反向还原逐字节相等；代价是 6.2 命中 14 → 17（见上一行）。详见指南 §21 |
| ✅ **合并主键是否扩成 `(benchmark, score_type, config)`（D12 量化后否决）** | ~~B 档 5 组的根治办法~~ 本轮先量后决：把 `score_type` 加进主键，对残留 14 组的消解数实测 **0**（组内 `score_type` 本就相同），只增加语义歧义 | **不改**。B 档靠 `config` 里写清口径顶住即可（D11 已填）。另正一处原文笔误：主键**不在 `model_data_tool.py` 里硬编码**（`MergeStrategy` 键字段默认空、全部由 CLI 传入），改键是改 SOP 命令而非改代码，权威字符串见指南 §18 第 5 点 |
| 主库 positioning 空值 | 补合并后花名册填充率 89.5%，剩余多属确无适用标签 | 抽样复核即可 |
| 可视化发布 | `viz/viz_index.html` 需按最终库重新生成 | 合并定版后执行 |
| 交付 push | 主库已定版待推 | 用 `C:\Program Files\Git\cmd\git`（system credential.helper=manager，静默通过） |

---

## 7. 本轮产出与变更

| 文件 | 变更 |
|---|---|
| `model_data_v2.jsonl` | 补合并 277 条采集成果；修 3 条 `meta.verified_at` |
| `scripts/qa_stats.py` | **新增**：阶段 3 填充率统计器（含分型定价、占位符口径修正） |
| `scripts/diff_incoming_db.py` | **新增**：incoming vs 主库差异扫描器（冲突裁决清单输入） |
| `scripts/model_data_tool.py` | `_save()` 加重试退避（修 Windows 瞬时锁定）；`merge --incoming` 支持多文件/目录/`@清单` |
| `intermediate/qa_stats.json` | 质检统计明细 |
| `intermediate/conflicts.json` | 差异扫描明细 |
| `intermediate/merge_todo.txt` | 277 个待合并文件清单 |
| `backups/model_data_v2.jsonl.wb05-pre-qa-20260829-192705` | 合并前主库备份 |

---

*维护者：`workbuddy-05` ｜ 生成时间：2026-08-29*
