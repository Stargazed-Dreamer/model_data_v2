# 2026-09-02

- D17 / D17b / D17c 三遍结案：`free_tier.available` 由宽口径收窄为「vendor 官方 API 当前是否有免费额度」。各遍改判 67 / 45 / 3，累计 115 处、触及 120 条、notes 改 63 处；终态 是 13 / 否 92 / 空 110（D16 收尾时 120/36/59）。
- 第 20 轮拍板窄口径；第 21 轮追加四裁定：Q1 平台级额度不算到本条（23 条阿里云 → null + 待重采）、Q2 legacy ≠ retired、Q3 不要求 token 计量（amazon-q-developer 保 true）、Q4 vendor 自有 Free API endpoint 算（nvidia:nemotron-3-ultra 保 true）。
- 不变量：记录 933→933、跑分 5642→5642、`score` 零改动、形状仍 null 718 / dict 215、键序变体 1、越界路径 0；门禁三遍均 ERROR 0 / WARN 469。终态 md5 `5d2b4eef6fa4b9a2f5f9745ebfae550c`。
- 09-01 记的「遗留口径张力」已消解：官方 App 一律不算，gemini-3-flash 与另两条归一为 null。
- 六个判据盲区 + 审计脚本自身的量词作用域盲区（2 条具名豁免）+ 文档示例三处缺陷全记进指南 §26（拆 §26.1–§26.5 六个子节）+ v2.9 留痕。
- `prompt.md` 修五处：free_tier 示例改正例+反例（错了两代，`git log -S` 实证 D16 提交 b185006 就写错）、规范单行示例补 ERROR 级必采键 `cache_write`、`source_type` 字段说明与下拉表补「自报」取值（436 条存量 WARN 的抄写源）。
- 新增可执行护栏 `temp/d17c_example_gate.py`：把文档自己的 ```jsonl 示例喂门禁 + 负对照。实测 正 exit 0 / ERROR 0 / WARN 0，负 exit 1 / ERROR 1。
- `qa_report.md` 抬头十三→十四处、第 13 项被推翻的宽口径两句与 D16 分布快照加删除线、新增第 14 项。
- `docs/交接_2026-09-01_D16结案.md` 改正：远端是 GitHub **公共**仓库（原写「私有」），并在「绝对禁止」行补上不得提交 `docs/memory/`、`temp/`、`*.d1*bak-*`。
- 遗留登记 D18：26 条阿里云待重采 per-model 免费额度表；`open-mistral-nemo:2407`（true）与孪生条 `mistral-nemo-base:2407`（false，T0 退役表）事实对撞，原句逐字节保留待重采；436 条自报分 `source_type` 归一。

- D18 结案：自报分 `source_type` 补「自报」+ 裸 `T0` 可信度归位 + 门禁放过空值。第 22 轮四项拍板：范围 = 修能修的 + 门禁放过空值；原本带括号的 8 种写法（42 条）原样挂后缀、容忍双括号（保变换统一 → 逆还原只需剔后缀）；裸 `T0` 一并改（先按 31 条拍、实测是 122 条后停下来重报重问，改判「122 条全改」）。
- 写盘 423 个条目 / 73 条记录：`source_type` 改 308（R1 复用库里已有写法 156 / R2 换尾括号 `（转述）`→`（自报分转述）` 7 / R3 新增变体 145；保真断言「原写法含转述 ⇔ 新写法含转述」0 偏移，全程不删也不注入「转述」）、`confidence` 改 122（`T0`→`T0-自报`；裸 `T0` 只在自报分段，`independent`/`arena_elo` 各 0，122 条无一含转述 → 撞 ERROR 的风险改前实测 0）。
- 门禁只加两个字（`and stype`）：把「主张了中立来源」（写错）与「根本没主张来源类型」（缺失）拆成两类。**WARN 逐级记账 469 → 341 → 33**，两级差额 −128 / −308 各自恰为一个可独立测量的量；常量段 452 → 16。剩余 33 = knowledge_cutoff 格式 9 + 参数量缺未披露声明 3 + 定价来源类型与价格值矛盾 3 + 有定价缺 source_url 1 + 规则 6.2 主键撞车 17。
- 验收：记录 933→933、跑分 5642→5642、`score` 多重集相同、未改动行 860/933 逐字节相同、叶子差异越界路径 0（白名单仅 `benchmarks.self_reported[i].source_type|confidence`）、逆还原逐字节复原。md5 `5d2b4eef…50c` → `b5eaf743ebbe462613b3613e04168755`，备份 `model_data_v2.jsonl.d18bak-20260902-190135`。
- **本轮最该记住的是「报数口径」**：31 是在 436 条 WARN 组内数的，按缺陷类全库数真值 122 —— 另 91 条因 `source_type` 已写对而不触发任何门禁信号，从没进入过我的视野，用户等于在偏小 4 倍的数字上做了决定。同一轮同类陷阱又应验两次：128 vs 775（来源类型缺失：门禁可见子集 vs 全库）、39 vs 438（转述对撞：窄判据 vs 宽判据）。**规矩：报数必须说清判据与范围，并同时给出放宽判据后的数量；只写一个数等于给下一轮埋雷。**
- 复核脚本自己也会错：独立复核的逆还原报「与 HEAD 不逐字节相等」，根因是回填用 `ia.get("source_type")` 无条件赋值，而 `dict.get()` 对不存在的键返回 `None`，凭空造出 `source_type: null` 改了键集与键序。**凡逆还原报不等，先怀疑复核脚本，再怀疑数据。**
- 登记四类不修的缺口（指南 §27 末）：① 来源类型缺失 775 条（门禁可见仅 128，另 647 条连 `confidence` 键都没有）；② 转述对撞 窄 39 / 宽 438（本轮一条没减），且窄口径那 9 个串被挂了后缀**已搜不到**，§27 给出改后对应串并提醒它们不再唯一标识这 39 条；③ **门禁判据本身有个洞没堵** —— 那条 WARN 是纯子串匹配 `"自报" in source_type`，于是 `"source_type": "T0-自报"` 这种**把可信度值写进来源类型栏**的也能过门禁却零来源信息，实测 607 条 / 62 条记录（另 ~~405~~ **406** 条前缀冗余，**D18b 复算更正**），**这条 WARN 归零有一部分是空心的**；升语义判断要维护 191 种写法的白名单，成本未评估 → ~~存量待拍板~~ **D18b 已拍板「只加门禁留痕、数据一字不动」= 规则 6.3**，新增已在 `prompt.md` 用显式禁令挡住；④ 438 里最大一项 `T0-自报` 83 与 ③ 同源。
- 文档同步：指南新增 §27 + v3.0 留痕 + §26 遗留清单那条 436 加删除线；`prompt.md` 两处示例补 `（自报）` + 来源类型下拉说明按实测重写（门禁真实判据只是「含自报二字」，下拉三项是推荐不是穷举，库里现有 178 种含自报写法）+ 新增「别把可信度值当来源类型写」禁令；`qa_report.md` 新增第 15 项 + 抬头十四→十五处 + 第 1 项与 WARN 演进表的 469 加删除线；`docs/交接_2026-09-01_D16结案.md` 抬头加时效性横幅（该卡的「WARN 469 对账」指令已被 D18 取代，照它对账会误判现库坏了）。
- 修回一处自己造成的损坏：插 §27 时 Edit 的 old_string 吃掉了 `## 11. 并发上限（429）的处理` 标题，已恢复（用标题清单与 HEAD 逐条 diff 验证，只差新增的 §27 一条）。**教训：插入章节时 old_string 不要包含下一个标题，否则 new_string 忘了带回就静默删了它。**
- D18 收尾基线：**933 条 / ERROR 0 / WARN 33（= 常量段 16 + 规则 6.2 的 17）/ 结构漂移 0 / 精确主键重复 17 组**。旧文档里以 469 为基线的表述自此过时，但历史行的 469 是对的。**→ 同日 D18b 加规则 6.3 后为 WARN 640，见下面一组。**

## D18b（同日续 · 只加门禁留痕，零数据改动）

- D18b 结案：第 23 轮拍板「**只加门禁留痕，数据一字不动**」→ 门禁新增 WARN 级**规则 6.3**（跑分条目 `source_type` **整栏就是一个可信度等级值**即报）。判据用正则 `^T[0-4](-自报)?(-转述)?(-第三方)?$` 而**不是 `CONFIDENCE_ENUM` 成员判定** —— 实测 `T2-第三方` 3 条、`T3-转述` 1 条在枚举外，按枚举判会漏 4 条（607 → 603）。**枚举是「合法可信度值」的表，不是「长得像可信度值」的表**，这里要查的是后者。**三段都查**：`arena_elo` 段此前完全不读 `source_type`，该段那 3 条对旧门禁彻底不可见。正则只决定是否记一条 WARN，**绝不驱动数据改写**。
- **提请拍板前先量化，救回 399 条**：607 条按「同一条目有没有 `confidence` 键」分三档 —— 甲 **123** 两栏一致（置空零损失）、乙 **399** 连 `confidence` 键都不存在（`source_type` 是该条目唯一的等级线索，置空即销毁，涉 37 条记录）、丙 **85** 两栏矛盾（84 × `T0-自报` vs `T0-自报-转述`、1 × `T0-自报-转述` vs `T3`；判哪个错要知道真实转述源 → 属重采）。我最初就是按「607 条一律置空」去想的，**量完发现会毁掉乙档的唯一线索，于是停下来重报重问**。
- 验收：933 条 / ERROR **0** / WARN **33 → 640**（差额 **607** 恰等于独立脚本实测的规则 6.3 命中数；分段 `self_reported` 600 / `independent` 4 / `arena_elo` 3 逐项一致；值分布 `T0-自报` 379 / `T0-自报-转述` 200 / `T0` 22 / `T2-第三方` 3 / `T3` 2 / `T3-转述` 1）、**数据 md5 `b5eaf743ebbe462613b3613e04168755` 一字未变**、负对照「新门禁 × 改前备份 = WARN **948** = 341 + 607」证明这批 D18 既没造出也没修掉、旧门禁 × 现库 = 33 / 旧门禁 × 备份 = 341（四个组合各自可解释）、文档示例护栏仍 pos exit 0 / neg exit 1、剩余 33 条与新规则命中**零重叠**。**刻意不覆盖**冗余前缀类 **406 条**（信息不丢、只是前缀冗余；一条 WARN 只背一类缺陷）。**升 ERROR 的条件**沿用规则 1.1/1.2：下一个采集周期新记录命中 0 且存量已处置。
- **复算文档数字时撞到两种情况并存**：§27 的宽口径 438 我按三段全扫得 **440**，查下来是**我漏了「仅 `self_reported` 段 + 剔除空值」这个作用域，文档是对的**（放宽作用域后各口径为 440 / 458 / 460，已补写进判据原文）；而冗余前缀类文档写 **405**、实测 **406**，**这次是文档真错**。**规矩：不能因为一个数对上就放过后一个，也不能因为一个数错了就推翻另一个 —— 逐个查到底。**
- 另一条硬证：§27 登记的**窄口径 39 条，把判据原样重跑在改后库上得 0 条**（改前快照重跑仍是 39 条 / 9 种、分布逐项吻合）—— 判据已被 D18 的写盘动作作废，那 39 条只能按「记录 + 数组下标」定位，重跑判据找不回来。
- 遗留（规则 6.3 只负责照出来，不负责修）：乙档 399 条要的是**迁移**不是置空，且那 4 条枚举外写法逐字迁移会立刻造 ERROR（得先决定扩枚举还是改写成 `T2`/`T3`）；丙档 85 条属重采；406 条冗余前缀类无任何门禁信号；与 §27 第一类宽口径 438 条**重叠 83 条**（同一条目背两类缺陷，修时要一次修完）。
- 文档同步：指南新增 **§27.1** + v3.1 留痕 + §27 第四类加删除线指向 §27.1 + 抬头「D2–D14」改「D2–D18b」+ v3.0 历史行里那个 405 就地更正；`prompt.md` 步骤 3 的禁令从「已知盲区」改成「**规则 6.3 会报、新采集会立刻吃 WARN**」并更正 405→406；`qa_report.md` 新增**第 16 项** + 抬头十五→十六处 + 第 1 项/现状表/演进表三处基线；`交接_2026-09-01_D16结案.md` 横幅基线 33→640 并补 §27.1 与第 16 项指针。
- 现库基线：**933 条 / ERROR 0 / WARN 640（= 常量段 16 + 规则 6.2 的 17 + 规则 6.3 的 607）/ 结构漂移 0 / 精确主键重复 17 组**，数据 md5 与 D18 收尾时**完全相同**（本轮零数据改动）。**→ 同日 D18c 把能修的修掉后为 WARN 122，见下面一组。**

## D18c（同日续 · 乙档迁移 + 甲档清冗余，518 个条目）

- D18c 结案：第 24 轮拍板「**乙档迁移 + 甲档清冗余**」。三档身份本轮独立重算，与 D18b 登记的 123 / 399 / 85 逐项吻合：甲 = 可信度栏已有**同值** 123（全在自报分段）、乙 = **无可信度键** 399（自报 393 / 独立 4 / 竞技场 2）、丙 = 两栏**矛盾** 85（自报 84 / 竞技场 1）。
- **为什么值得做**：乙档 399 条根本没有 `confidence` 键（涉 37 条记录），等级只写在来源类型栏 → 「任何跑分结论不得仅基于 `T0-自报`」这类规则对它们**无从执行**，下游读不到可信度。迁移是把信息搬回它该在的栏目，不是清洗。
- 变换两条（都可逆）：乙档 **395** 条 `confidence` ← 原 `source_type` 值（**逐字搬运，不改写不归一**），新键插在 `source_type` **紧后面**（库里 4198/4351 条同时有两键时就是这个顺序），随后 `source_type` ← `null`；甲档 **123** 条只把 `source_type` ← `null`，就地赋值，键序键集都不变。丙档 **85 条一字未动**（属重采）。
- **表外 4 条原样不动**（`T2-第三方` 3 + `T3-转述` 1）：逐字迁移会立刻把 WARN 换成 **ERROR**（不在 `CONFIDENCE_ENUM` 内），机械改写成 `T2`/`T3` 又等于替采集者做判断 —— 这 4 条的等级**本身就有争议**：`cognition:swe-1-6:base` 自报[1] 网址是头条、备注明写「转述自 Cognition 官方博客」，按决策 2 本该记 `T0-自报-转述`，记 T3 等于把**厂商的话**当成**媒体的话**；`mistral:mistral-large-3:base` 独立[5][6][7] 三条同址（OpenRouter）同标 `T2-第三方`，其中 [5] 备注明写「Artificial Analysis 评测」而 §5.1 把 AA 列为 **T1** 的典型例子 → 该记 T1 还是「T1 经转述」机械推不出来。
- **置 `null` 而不是删键**：① `prompt.md` 明文「缺失一律填 null」；② 键留着 → 键序不变 → **逆还原只需按值回填，不必记插入位置**；③ 与 D17 的 `available: null`、D16 的五键 `free_tier` 同口径。**代价**：来源类型缺失从此有**两种形状**（原本「无键」775 + 本轮「值为 null」515）→ 将来测缺失数必须两种都数，只数一种会少 515。
- 验收（**全部先在内存里跑完才落盘**）：前置断言现盘 md5 == 备份 == `b5eaf743…8755`、**序列化往返 933/933 逐字节相同**（本库是 `ensure_ascii=False` + **默认分隔符**，不是压缩写法 —— `separators=(',',':')` 实测匹配 0/933；先验过才敢拿「未改动行逐字节」当 rail）、记录 933→933、跑分条目 5642→5642、`score` 多重集相同、未改动行 **880/933 逐字节相同**（改动落在 53 条记录）、叶子级 diff **越界路径 0**（白名单仅 `benchmarks.<段>[i].source_type|confidence`；差异 **913** 处 = 乙档 395×2 + 甲档 123×1）、**逆还原与改前逐字节相等**（删 `confidence` 用 `del` 而非置 `null`，否则留下原本没有的键）、乙档新增键越位 0、迁移保真四项全 0、甲档「可信度栏被改动 0」、**改后仍被规则 6.3 照出的 89 条 == 丙档 85 ∪ 表外 4**（按 记录+段+下标 逐条比身份）**且这 89 条一字未动**、未参与的 5124 条被改动 0、`benchmarks` 以外差异记录 0、门禁 **933 / ERROR 0 / WARN 122** exit 0、文档示例护栏 `temp/d17c_example_gate.py` 仍 pos 0 / neg 1。
- **WARN 记账（比总数更硬的是「其余各类逐条不变」）**：规则 6.3 **607 → 89（−518）**，规则 6.2 主键撞车 17→17、`knowledge_cutoff` 9→9、参数量缺未披露声明 3→3、`pricing.source_type` 与价格值矛盾 3→3、有定价缺 `source_url` 1→1 → **差额 518 全部落在规则 6.3 一类**，可执行等式 **640 − 122 = 518 = 改动条目数**。另：`建议体现「自报」属性`那条改前改后都 **0** 命中（迁移后 22 条可信度变成裸 `T0`，但来源类型栏已置空，判据有 `and stype` 前置）—— **这正是备选方案「只补可信度栏、来源类型栏留着」会多吃 22 条 WARN（640 → 662 且规则 6.3 一条不减）的原因**，提请拍板时已量化。
- 改后面存量：可信度栏有键 4540 → **4935**；来源类型缺失 775 → **1290**（自报分段）/ 782 → **1300**（三段）；规则 6.3 命中 607 → **89**（自报 85 / 独立 3 / 竞技场 1）；非空写法 237 → **235 种**（`T0` 与 `T3` 两种自此在库里消失 —— 它们的每一处出现都被本轮置空了）。
- **两条教训**：① **把信息从 A 栏搬到 B 栏之前，先确认 B 栏的合法值表能接住 A 栏的写法** —— 接不住的那几条要单独拎出来，不要为了让变换统一而改写它们（本轮若按「一律迁移」动手，那 4 条会把 WARN 换成 ERROR、门禁对全库直接失败）。② **打印语句与断言不一致时以断言为准，但那行打印必须当场修掉** —— 收尾核对脚本第一版打印「改后命中里本轮被改过的条目数：89（必须为 0）」而紧随的断言通过，因为打印用了 `eo[k] is not en[k]`（对两个分别解析出的 dict 恒真）；断言才是取证件，打印只是给人看的，但不修的话下一个人会照着那个 89 写文档。
- 文档同步：指南新增 **§27.2** + v3.2 留痕 + 抬头「D2–D18b」改「D2–D18c」+ §27 基线行与 §27.1 的乙档遗留/83 条重叠两处就地更新（**D18c 复核：那 83 条全落在丙档，丙档一字未动 → 重叠一条没减**）；`prompt.md` 步骤 3 的 607 分布划掉换成 **89 条 / 12 条记录** + **可信度等级行下新增「七个值是穷举，写枚举外值直接 ERROR」禁令**（附表外 4 条两处争议当反例）；`qa_report.md` 新增**第 17 项** + 抬头十五→十七处 + 第 16 项乙档遗留划掉指向第 17 项 + §1/§6.2 三处基线 + 抬头 `temp/*.py` 声明的轮次范围 `D2–D15` → `D2–D18c`；`交接_2026-09-01_D16结案.md` 横幅基线 640 → 122、补新 md5 与 §27.2/第 17 项指针、并把「照 469 或 33 对账会误判」扩成「照 469 / 33 / 640 都会误判」。
- 数据 md5 `b5eaf743ebbe462613b3613e04168755` → **`3c88eec4b25afe2e107e4fc5d1a84d77`**（8156418 → 8162953 字节），备份 `model_data_v2.jsonl.d18cbak-20260902-201552`（`.gitignore` 已忽略）。**门禁脚本本轮一字未改**（md5 `f70bcb836488860eb2708dd67625449e`）。
- D18c 收尾基线：**933 条 / ERROR 0 / WARN 122（= 常量段 16 + 规则 6.2 的 17 + 规则 6.3 的 89）/ 结构漂移 0 / 精确主键重复 17 组**。仍开着：表外 4 条（等级争议）、丙档 85 条（属重采，其中 83 条与宽口径 438 重叠一条没减）、**406 条冗余前缀类无任何门禁信号**、来源类型缺失涨到 1290 / 1300 且**对门禁完全不可见**。

## D18d（同日续 · 剥掉来源类型栏的等级值前缀，405 个条目）

- 第 25 轮拍板 **「全剥 406 + 补挂「自报」+ 6 条粘连的迁进可信度栏」**。缺陷是 `"source_type": "T0 官方自报"` 这种把可信度等级**当前缀缀在真实来源描述前**的写法 —— 信息不丢、只是前缀冗余，但一栏背两种语义，也正是 D18b「规则 6.3 刻意不覆盖」留下的那批。
- **口径按构造给出，不靠正则猜**：前缀类 = 以等级开头（非空 ∧ `^\s*T[0-4]`）**−** 纯等级值（规则 6.3 的正则）= **406**；与 D18b 在另一快照上记的 `1013 − 607 = 406` 同数 → 反证 D18c 一条没碰过这类。前缀抽取用**长度降序的 25 种等级写法表做最长匹配**再吃分隔符，不用会回溯的正则量词（第一版 `T[0-4](-自报)?…[sep]*` 会把 `T0-自报 官方一手` 回溯成前缀 `T0` + 剩余 `-自报 官方一手`，于是窄口径 495 与规则 6.3 的 89 **重叠**，违反「一条 WARN 只背一类缺陷」）。范围 **52 条记录 / 32 种写法**（自报 399 / 独立 7）。
- **本轮最该被读到的发现**：406 条里**没有一条是真正的两栏矛盾** —— 可信度栏同值 84、比前缀更具体 316、无键 6，即前缀携带的等级信息在同一条目的 `confidence` 里逐条都能读到 → 剥掉零损失（与丙档 85 条属两类，别混）。
- 三条变换规则：① 剥前缀 + 吃分隔符；② 粘连写法 `T0-自报-技术报告` **5 条**走 D18c 的迁移（等级值搬进 `confidence`、新键插在 `source_type` 紧后面）；③ 自报分段 ∧ 可信度在 T0 家族 ∧ 剥完不含「自报」→ 末尾补挂「（自报）」，实测 **55 条**。**实写 405 条 / 51 条记录**（只改来源类型栏 400 + 同时改两栏 5）。**表外 1 条原样不动**：`T1.5-第三方评测`（`deepseek:deepseekmoe-16b:base` 独立[0]）—— `T1.5` 不在 `CONFIDENCE_ENUM` 内，迁移造 ERROR、改写成 `T1`/`T2` 等于替采集者做判断。
- **备选方案副作用已量化**：「只剥不补挂」会让 **55 条**丢自报标记 → 新触发「建议体现自报属性」WARN 55 条 → **122 变 177**；本方案新增 **0**。（此前登记的 66 是**另一个口径**「剥完不含自报」而不管原值有没有；真会撞门禁的是 **50**，按 13 种口径试探后取 D == E == 50 交叉验证。）
- 验收：前置断言现盘 md5 == `git HEAD` blob == 备份 == `3c88eec4…d77`、序列化往返 933/933 逐字节、记录 933→933、跑分 5642→5642、`score` 多重集相同、未改动行 **882/933**、叶子 diff **410 处 / 越界路径 0**（白名单仅 `benchmarks.(self_reported|independent|arena_elo)[i].(source_type|confidence)`，出现 ≥3 个差异叶子的条目 **0**）、**逆还原 405/405**、**可执行等式「以等级开头 495 → 90 = 纯等级值 89 + 表外 1，差额 405 == 改动条目数」**、**形状三分 4342 + 782 + 518 == 5642 改前改后各自成立**（本轮既不新造 null 也不删键）、表外那条逐键相同、门禁 **933 / ERROR 0 / WARN 122 → 122** exit 0、示例护栏仍 pos 0 / neg 1、**门禁脚本 md5 `f70bcb83…449e` 一字未动**。
- **信息零损失按可执行判据逐条论证**（不是断言「应该没丢」）：被剥掉的头部里那个等级值改后必须仍能在同一条目的 `confidence` 里读到（同值或更具体）→ 不满足 **0** 条，分布**更具体 316 / 同值 89**。
- **最强也最该如实说的证据**：改前改后两份门禁报告**除标题里的文件名外逐字节相同**（各 194 行、六类 WARN 逐项一致）→ 这轮改动**对门禁完全不可见**，价值纯语义，不能包装成质量指标提升；**推论是现在没有任何机制阻止它回归**，只有 `prompt.md` 的禁令在守，而文档自己错过两代（§26.5）。
- **本轮建立的新保真不变量**：含「自报」的**条目数 2567 → 2567 一条不变**，而**写法种类 178 → 171** —— 两个数必须一起看，否则「归一重复写法」与「给条目加上/摘掉自报属性」在验收上无法区分。
- 改后存量：非空写法 235 → **228 种**、可信度栏有键 4935 → **4940**、前缀类命中 406 → **1**（只剩表外那条）、规则 6.3 **89 → 89**（本轮一条不碰纯等级值）、来源类型缺失 **1300 → 1300**（无键 782 + 值为 null 518，两种形状都数了）。
- **四条教训**：① **验收脚本自己出 bug 造成假警报已是第三次**（D18 `.get()` 凭空造键、D18c `is not` 比两个 dict 恒真，本轮是无条件剥尾部「（自报）」—— 那 20 条 `T0 官方一手技术报告（自报）` 的后缀是 **D18** 挂的不是本轮挂的，于是报「旧值不以新值主体结尾」）；修法是把逆还原写成两分支并**断言两分支不会同时成立**（歧义 **0**）—— 若同时成立头部就有两种拆法、整道 rail 失效，**所以「唯一性」本身要当断言写，不是修完就算**。② **报数前先确认判据的作用域，这已是第三次栽在同一处**：按「仅自报分段」预测缺失形状 775 / 515，而扫描器覆盖三段 → 真值 782 / 518；已补 `4342 + 782 + 518 == 5642` 当自洽检查。③ **用集合比，不用计数比**：探针里 `len(trig) == len(t0self)` 打印「两者相等」（50 == 50）但**集合不同**（trig 里 5 条前缀是 `T0-自报-转述`）—— 与 D18c 教训 ② 同一失败模式。④ **「WARN 没动」不等于「这轮什么都没做」**，但要拿得出证据：逐字节同构的两份报告 + 一条语义不变量。
- 另记一处**正则字符类陷阱**：`[ \t:：、,，)）-—]` 里 `）-—` 被解析成范围（U+FF09→U+2014）→ `re.error: bad character range`，把 `-` 挪到末尾才对。**差点更糟**：若前面是 ASCII `)`，那会是个**合法**的反向范围，静默匹配掉约 8000 个 CJK 字符 —— 报错比沉默安全。
- 文档同步：指南新增 **§27.3** + v3.3 留痕 + 抬头「D2–D18c」改「D2–D18d」+ §27.1 两处 406 表述（「刻意不覆盖」与「无任何门禁信号」）加删除线指向 §27.3；`prompt.md` 的 178 种划掉换成 **171 种（D18d 后）**+ 新增「种类数会降、含自报的条目数不该动」验收口径（2567 → 2567）、406 条那段划掉换成结案块 + **新增前瞻禁令「新采集不得把等级值当前缀写进 `source_type`」**并写明「目前没有任何门禁规则照这类，这条禁令只有本文档在守」；`qa_report.md` 新增**第 18 项** + 抬头十七→十八处 + 第 17 项遗留 ③ 划掉指向第 18 项 + §1/§6.2 三处基线 + 抬头轮次范围 `D2–D18c` → `D2–D18d`；`交接_2026-09-01_D16结案.md` 横幅补 D18d、md5 链推进、并加一句「WARN 仍是 122 但 md5 变了，别拿『WARN 没动』推断『数据没动』」。
- 数据 md5 `3c88eec4b25afe2e107e4fc5d1a84d77` → **`3553105b811c7104199d7de2b8052e91`**，备份 `model_data_v2.jsonl.d18dbak-20260902-210735`（`.gitignore` 已忽略）。**门禁脚本本轮一字未改**。
- D18d 收尾基线：**933 条 / ERROR 0 / WARN 122（= 常量段 16 + 规则 6.2 的 17 + 规则 6.3 的 89）/ 结构漂移 0 / 精确主键重复 17 组**。仍开着：表外 `T1.5-第三方评测` 1 条（本轮新登记）+ 枚举外 4 条（等级争议）、丙档 85 条（属重采）、**前缀类无任何门禁规则**（是否补 WARN 级规则 6.4 属单独拍板，现库命中会是 **0**，纯防回归）、5 条 `（官方技术报告）（自报）` 双括号观感遗留、**这一栏仍不收敛**（228 种写法里 D18d 新造 24 种，31 种剥离结果里只有 7 种是库里原有的）。

## D18e（同日再续 · 门禁补 WARN 级规则 6.4，零数据改动）

- 第 26 轮拍板 **「补，用『等级值 + 分隔符 + 主体』形」**，处置 D18d 遗留 ③：存量剥净之后**没有任何机制阻止新采集把它写回来**，挡它的只有 `prompt.md` 一句禁令，而 §26.5 已实证文档自己错过两代、且门禁只校验记录不校验文档。
- **判据形三选一，先把三个形都在现库与改前备份上量过再提请拍板**（不是先拍再量）：A「以等级开头就算」（非空 ∧ `^\s*T[0-4]` ∧ ¬纯等级值）现库 **1** / 备份 **406**；B「等级值 + 分隔符 + 主体」**未加前置**时现库 **89** / 备份 **494**、与规则 6.3 **重复计 89 条**；**B′ = B + ¬纯等级值前置（选定）** 现库 **0** / 备份 **405**、与 6.3 **零重叠**；C「只看开头」最宽，现库 **90** / 备份 **495**。**四个形的差额都是 405，恰等于 D18d 的实写条目数** → 反过来给 D18d 又添一道独立负对照。
- **选 B′ 不选 A 的理由**：A 形现库那 1 条命中就是 D18d 刻意不动的表外值 `T1.5-第三方评测`，会把基线推到 **123**，而且是**以「前缀冗余」的名义**报一条真缺陷是「等级值不在 `CONFIDENCE_ENUM` 内」的条目 —— 与 D18b/D18e 两次援引的「一条 WARN 只背一类缺陷」直接冲突。B′ 照不到它（`T1.5` 后接 `.` 不是分隔符）**是有意的**。
- **三条实现要点**（都写在 `TIER_PREFIX_STYPE_RE` 上方注释里，不只写在文档里）：① 调用处那道 `and not TIER_ONLY_STYPE_RE.match(stype)` 前置**是必需的、不是保险** —— 分隔符表含 `-`，少了它 `"T0-自报"` 会被解析成「`T0` + 分隔符 `-` + 主体 `自报`」而**同时**命中 6.3，重复计 **89 条**（上表 B 形那行就是这个 bug 的实测量）。② 分隔符字符类里 `-` **必须放最末**（`[ \t:：、,，)）—-]`），写成 `)）-—` 会报 `bad character range`；**更糟的是若 ASCII `)` 排前面那是个合法反向范围，会静默匹配约 8000 个 CJK 字符**（报错比沉默安全，D18d 已栽过一次）。③ **跑分三段都查** —— `arena_elo` 段此前完全不读 `source_type`，D18b 补 6.3 时才纳入，6.4 同样要查（改前备份实测本段命中 **0**，但判据不能靠这个省略）。
- 验收（零数据改动，rails 全在门禁侧）：**2×2 负对照矩阵** —— 旧门禁 × 现库 **122**、旧门禁 × 改前备份 **122**（印证 D18d「两份报告除标题外逐字节相同」）、新门禁 × 现库 **122**（6.4 命中 0）、新门禁 × 改前备份 **527 = 122 + 405**；四组都是 933 条 / ERROR 0 / exit 0。现库 WARN 构成逐项复核 89 + 17 + 16 + **0** = **122**。规则 6.4 在备份上命中 **405**（自报 399 / 独立 6 / arena 0），其中 9 条同时带一条 6.2 WARN —— 不影响算术，因为 WARN 按**消息**计不按条目计。
- **本轮最该被读到的一道：五个探针的正对照。** 现库命中 **0** 意味着「这条规则真的会响」在真库上**拿不到任何证据** —— 写错变量名、放错循环、正则不匹配都会静默表现为「命中 0」，与「库里真干净」在门禁输出上**完全无法区分**。所以必须自己造一条带前缀的记录喂门禁。载体 `alibaba:qwen2-1-5b:base`（全库 **95** 条合格载体之一），三条挑选条件：原值非空且本身不触发 6.3/6.4、`confidence` **不在 T0 家族**（否则「建议体现自报属性」会跟着 `source_type` 变、混淆计数）、整条记录无任何 6.3/6.4 命中。**P0** 原样重写 → WARN **122** 且临时文件与真库 **md5 逐字节相同**（归因前提：若重写本身引入差异，P1–P4 的计数差里就混着序列化噪音）；**P1** `T0 官方自报` → **123** 只 6.4 响（规则接通）；**P2** `T0-自报` → **123** 只 6.3 响（互斥）；**P3** `T1.5-第三方评测` → **122** 两条都不响（「刻意不覆盖表外值」真生效）；**P4** `官方技术报告（自报）` → **122** 两条都不响（本该这么写的不误伤）。五个全 PASS。
- **一条教训（本轮唯一，但已是第四次同类）**：**验收脚本自己的判据错，会报出与被验对象无关的假警。** 探针第一版五个里四个 FAIL，看着像「规则 6.4 没接通或互斥性破了」，实际门禁全对 —— 错在探针用「6.3 的消息文本 in 整份报告」做**全报告范围**子串匹配，而真库本来就有 **89 条**规则 6.3 的 WARN，这句恒真。修法是**按 `## \`model_id\`` 标题精确截出载体自己那一段再搜**（注意 `alibaba:qwen2-1-5b:base` 与 `alibaba:qwen2-0-5b:base` 只差一个字符，子串匹配会串到隔壁段），并补一道防呆 **期望至少一条要响时载体段必须非空** —— 否则截段失败静默返回空串，让 P0/P3/P4 假通过、P1/P2 假失败、诊断信息还指错方向。这是 D18 的 `.get()` 凭空造键、D18c 的 `is not` 比两个 dict 恒真、D18d 的无条件剥后缀之后的**第四次**。规律补一句：**判据的作用域要与被验对象的作用域一致 —— 验一条记录就在那条记录的段落里搜，别在整份报告里搜。**
- **升 ERROR 的条件**沿用规则 1.1/1.2 与 6.3（下一个采集周期新记录命中 0 且存量已处置 —— 现库已经是 0，所以这条件从加上那一刻就成立一半），但**本轮刻意不升**，与 1.1/1.2/6.3 停在 WARN 的口径一致。
- 文档同步：指南新增 **§27.4** + v3.4 留痕 + 抬头「D2–D18d」改「D2–D18e」+ §27.3 遗留 ② 加删除线指向 §27.4；`prompt.md` 把 D18d 写下的「目前没有任何门禁规则照这类，这条禁令只有本文档在守」划掉换成 D18e 实况（6.4 会报、现库命中 0、与 6.3 互斥、刻意照不到表外那条），指针扩到 **§27.4**；`qa_report.md` 新增**第 19 项** + 抬头十八→十九处 + 第 17 项遗留 ③ 与第 18 项遗留 ③ 划掉指向第 19 项 + §1/§6.2 三处基线 + 抬头轮次范围 `D2–D18d` → `D2–D18e`；`交接_2026-09-01_D16结案.md` 横幅补 D18e、md5 链推进、并把那句反直觉提醒从单向改成双向（两个方向都不能拿「WARN 没动」下结论）。
- **数据 md5 `3553105b811c7104199d7de2b8052e91` 一字未变**（本轮零数据改动、没建备份）；**门禁脚本 md5 `f70bcb836488860eb2708dd67625449e` → `6ea4de427c0875375b9fd83f6f0cf1c3`**（D18c/D18d 两轮都没动过它，本轮是这三轮里唯一一次改门禁；改前那一版 = `git show HEAD~1:scripts/validate_model_data.py`）。
- D18e 收尾基线：**933 条 / ERROR 0 / WARN 122（= 常量段 16 + 规则 6.2 的 17 + 规则 6.3 的 89 + 规则 6.4 的 0）/ 结构漂移 0 / 精确主键重复 17 组**。**WARN 与 D18d 收尾时相同但原因相反**：D18d 是「改了数据而门禁照不到」，D18e 是「改了门禁而现库本来就干净」—— 读的人靠**哪个 md5 动了**分辨。仍开着：表外 `T1.5-第三方评测` **1 条**（6.4 刻意照不到，与 D18c 表外 **4 条**同类，合计 **5 条**枚举外可信度值待单独处置）、丙档 **85 条**（属重采）、来源类型缺失 **1300**（三段、两种形状，门禁放过、只能靠清单追踪）、**这一栏仍不收敛**（228 种写法）、5 条双括号观感遗留。

## D19（同日再续 · 来源类型栏 5 条「枚举外可信度值」逐条定级，5 个条目）

- **第 27 轮拍板原文**：**「按建议逐条定级」**。处置 D18c 与 D18d 两轮**都刻意原样不动**留下的同一批 5 条表外值 —— 逐字迁进 `confidence` 会直接造 ERROR（不在 `CONFIDENCE_ENUM` 内），机械改写成 `T1`/`T2`/`T3` 又等于替采集者做判断，所以只能逐条读**本条记录自己的**证据定级。
- **五条定级与依据**：`cognition:swe-1-6:base` 自报[1] `T3-转述` → **`T0-自报-转述`**（决策 2 明文强制：厂商自报分经媒体转述且官方报告不可直访必须标此值；原记 `T3` 把「转述载体」当成了「可信度」）；`mistral:mistral-large-3:base` 独立[5] `T2-第三方` → **`T1`**（本条 notes 明写 Artificial Analysis 评测，§5.1 把 AA 列为 T1）；同记录独立[6]/[7] → **`T2`**（**本条无归属证据，禁止抄兄弟条目 [5]**，只按 url 是 OpenRouter 转述定级）；`deepseek:deepseekmoe-16b:base` 独立[0] `T1.5-第三方评测` → **`T3`**（§5.1 距离原则：媒体报道第三方评测是二手转述；`T1.5` 这个等级根本不存在）。
- **变换**沿 D18c 乙档：`confidence` ← 定级值、新键**紧插在 `source_type` 之后**（4198/4351 的主流键序 → 键序不变、逆还原只需 `del`），随后 `source_type` ← **`null`**（不删键）登记待重采。
- **三档判据口径**（按用户「报数必须说清口径并给放宽后的数」的规矩各扫一遍，中口径挖出一类从没登记过的缺陷）：窄口径「整栏就是一个枚举外等级写法」**5**；中口径「栏内任意位置出现等级形状」**111**（开头形 90 已被 6.3 / 6.4 / 本轮覆盖 → **差额 21 条是括号后缀形**，见下）；宽口径「递归扫全记录所有字段」命中 **2** 条但**全不在 `source_type`**（两条 gemini 的 `pricing.notes` / `meta.notes` 自由文本）→ **字段级判据必须按字段路径扫，递归扫自由文本会造出「还有遗留」的假命中**。
- **验收全绿**（`temp/d19_apply.py`，rails 先在内存跑完才落盘）：现盘 md5 == HEAD blob == `3553105b…`、门禁脚本 md5 仍是 D18e 那版 `6ea4de42…`（本轮不该碰它）、序列化往返 933/933 逐字节、五条旧值与「原本无 `confidence` 键」逐条锁定、五个新值全在枚举内、记录 933→933 / 条目 5642→5642 / `score` 多重集相同、叶子 diff **10 处 / 越界 0**、未参与 **5637 条改动 0**、**逆还原逐字节相等** + 「实际执行 5 次而非 0 次」计数防呆、含「自报」条目 **2567 → 2567**（D18d 那道 rail 仍成立）。
- **首次把 `model_id` 全库唯一写成断言**：D6–D18e 十几轮的脚本全都**隐含**用它定位记录而从没验过，而库里明明有 17 组重复主键 —— 一直成立的假设不是公理，要么写成断言要么按索引定位。
- **WARN 记账：122 → 118，−4 全部落在规则 6.3（89 → 85），其余六类逐条不变。是 4 不是 5** —— `T1.5` 那条本就在 6.3/6.4 判据之外，它减掉的是「表外」这一类缺陷而不是 WARN。别拿「改了 N 条」预测「WARN 减 N」。
- **验收脚本自己第五次出 bug，这次是「假失败」**：断言写成 `(mb + mvb, ma + mva) == ((782,518),(782,523))`，而 `mb + mvb` 是**整数加法**（1300），跟元组比恒不等 → rail 报错，同一条语句打印的四个数**全对**。前四次：D18 `.get()` 凭空造键、D18c `is not` 比两个 dict 恒真、D18d 无条件剥掉别轮的 suffix、D18e 探针在整份报告里搜只属于一条记录的消息。规律补全：**判据的作用域与操作数的类型，都要和被验对象一致；「假失败」与「假通过」一样贵，它会把人推向错误的方向去改本没有问题的代码。**
- **改后存量**：非空 4342 → **4337**、值为 null 518 → **523**、无键 782 → **782**（本轮不新造无键）、来源类型缺失（三段）1300 → **1305**、可信度栏有键 4940 → **4945**、三段全扫非空写法 **228 → 225 种**（含「自报」的 171 种与 2567 条**都不动**，本轮那 5 条写法不含「自报」）。`source_type` 栏**枚举外等级写法现为 0**。
- **数据 md5 `3553105b811c7104199d7de2b8052e91` → `ceff15e9a12709ab3d49679f3d1560f5`**，备份 `model_data_v2.jsonl.d19bak-20260902-224225`；门禁脚本 md5 一字未动。
- **D19 收尾基线：933 条 / ERROR 0 / WARN 118（= 规则 6.3 的 85 + 规则 6.2 的 17 + 常量段 16 + 规则 6.4 的 0）/ 结构漂移 0 / 精确主键重复 17 组 / 名册 685-702。**
- **仍开着（下一项证据已备齐）**：① **等级值当「括号后缀」写在来源类型栏 21 条 / 3 条记录**（`官方模型卡自报（T0 直采）` 10 / `官方自报（T0 官方一手）` 7 / `官方论文自报（T0 直采）` 3 / `官方 GitHub 自报（T0 直采）` 1；记录 `deepseek:deepseekmath-v2:base` / `meta:llama-2-7b:base` / `mistral:magistral-small-1-0:base`）—— 是 D18d 前缀类的**镜像**，`confidence` **全是 `T0-自报`**、与括号里的 `T0` **一致 21 / 不一致 0 / 无键 0** → 与 D18c 甲档同类，**剥掉是零损失而不是「迁移」**；剥完主体仍含「自报」→ **新增那条 WARN 0 条**；**至今无任何门禁规则照它**。② 丙档 85 条属重采（= 规则 6.3 现在的全部命中，也是它升 ERROR 的唯一阻塞项）。③ 来源类型缺失 1305。④ 该栏仍不收敛（225 种）。⑤ 5 条双括号观感遗留。
- **文档同日改**：指南新增 **§27.5** + **v3.5** 留痕 + 抬头「D2–D18e」改「D2–D19」+ §27.4 遗留 ①② 加删除线；`prompt.md` 三处 —— 规则 6.3 现值 89 → 85、D18c/D18d 两处「表外待单独处置」划掉换成结案、可信度枚举那一块补定级结果与**新口径「定级不得抄兄弟条目」**、并新增「括号后缀形 21 条 6.3/6.4 都照不到」的禁令、种类数两口径（171 vs 225）分开写明；`qa_report.md` 新增**第 20 项** + 抬头十九→二十处 + 第 17/18/19 项遗留 ① 划掉 + §1/§6.2 基线同步 118；**新增 `docs/交接_2026-09-02_D19结案_换平台接手卡.md`**（用户额度见底、要换平台且上下文全丢 → 为零上下文读者写的全面落盘档）。记忆两处同步。


## D20（同日续 · 第 28 轮拍板 · 裁决先落盘再动手）

- **拍板原文（两问一并答）**：① 等级值当「括号后缀」写在来源类型栏的存量 —— **「剥等级、留限定词」**（`官方模型卡自报（T0 直采）` → `官方模型卡自报（直采）`，整段括号不删，因为「直采」「官方一手」是采集者写的来源限定词，删掉不算零损失）；② 是否补 WARN 级规则 6.5 照后缀形 —— **「补，并造探针」**（现库这一类命中会是 0，纯防新采集回归；按 D18e 的规矩，命中 0 的新规则必须自造正对照探针才算验收）。
- **动手前必须先重测（D19 的两张数字是改前快照口径）**：`111 / 90` 是**D19 改前盘**上的中口径与「以等级开头」，D19 置空 5 条后现盘应为「以等级开头 85」→ 中口径预测 106、差额仍是 21。**按预测值动手是错的，先量再说。**
- **实测后动手（①）**：判据形先量后拍 —— 四个候选形（S1 括号内含等级 / S2 尾部括号内含等级 / S3 任意位置含等级 / S3′=S3∧¬6.3∧¬6.4）× 三快照（现盘 / D20 改前备份 / D18d 改前备份）命中**完全相同**：现盘 **0**、两个备份**都是 21** → 选最宽的 S3 不付额外代价；而**去掉「不以等级开头」这道前置**四形分别变 85 / 106 / 516，会把 6.3 的现库命中重复报一遍（118 → 203），**前置是必需的不是保险**（D18e 躲开的同一个坑）。显式追加 `¬6.3 ∧ ¬6.4` 不改变命中集（那两条都要求等级在位置 0）→ **不写进代码**。
- **反直觉的历史事实**：把这条判据在**全部 26 个备份**上跑，从最早的 5467 条目那份到现盘**命中数一字不变全是 21** → 这类写法**从建库就在、17 轮整改一次没碰过它**。**推论：判据只要写成「以 X 开头」，就一定有镜像形活着**；今后每加一条位置敏感判据，当场把它的镜像形与中间形各扫一遍并记命中数。
- **数据改完（21 条目 / 3 记录 / 全在 `self_reported`；`meta:llama-2-7b:base` 10、`mistral:magistral-small-1-0:base` 7、`deepseek:deepseekmath-v2:base` 4）**：叶子差异 21 处越界 0、未参与 930 条一字未动、逆还原 21/21 逐字节、形状三分（4337/782/523）改前==改后、含「自报」2567→2567、写法 225→225（**4 进 4 出**且 4 种新写法库里原本都没有）、**改后复扫 0 与改前复扫 21 成对断言**。数据 md5 `ceff15e9…` → **`be9985e1d54a20b3734d2746766e742c`**（8161763 字节），备份 `…d20bak-20260902-231130`（= D19 版），**这一步门禁报告逐字节没变**（= D18d 模式）。
- **门禁改完（规则 6.5，WARN 级，跑分三段都查）**：`TIER_ANYWHERE_STYPE_RE` + 前置 `TIER_LEADING_STYPE_RE`（刻意比 6.3/6.4 的 `^T` 宽一档、多容 `^\s*`：宁可漏一格排版瑕疵也不把一类缺陷报成另一类）。门禁脚本 md5 `6ea4de42…` → **`4ab840522d03156c31a0c90c4eb2b783`**，**WARN 仍是 118**（= D18e 模式）。同日两个「WARN 不变」挨着发生、原因相反，**只能靠哪个 md5 动了分辨**。
- **十个探针全 PASS（`temp/d20_probe.py`）**，载体 `alibaba:qwen2-1-5b:base`（92 条合格里取第一条）：P0 与真库逐字节相同；P1 原写法 / P2 半角括号+`T2` / P3 小数 `T1.5` → **119 且仅 6.5 响**；P4 纯等级值 → 仅 6.3；P5 前缀形 → 仅 6.4（互斥性）；P6 前导空格 / P7 字母粘连 `XT0` / P8 剥后正确写法 / P9 正常写法 → **118 三条都不响**。**判据改为 `import validate_model_data` 直接引用上线版正则** —— D18e 那版探针抄了第二份 6.4 正则且与上线版不一致，**测自己抄的那份等于没测上线的那份**。负对照 = 新门禁 × D19 版备份 **139 = 118 + 21**。
- **本轮两处判据踩坑**：① 负对照 diff 直接数 `>` 行数出 29 → 多出的 6 行是 3 条记录的分段标题 + 3 个空行，**结构性行不是命中行**，比对报告差异要先归一化；② 「同类第六次」= **拿上一轮记录的数字当本轮动手依据**（`111/90` 是改前快照，现盘实测 `106/85`，两轮差额都恰好 21 纯属巧合）。
- **新登记一处理论盲区（不动门禁）**：`" T0 官方自报"` 这种带前导空格的前缀形**三条规则都不响**（P6 实测），现库 **0 条**。
- **D20 收尾基线：933 条 / ERROR 0 / WARN 118（不变：常量段 16 + 规则 6.2 的 17 + 6.3 的 85 + 6.4 的 0 + 6.5 的 0）/ 结构漂移 0 / 主键重复 17 组 / 名册 685-702。**
- **文档同日改**：指南新增 **§27.6** + **v3.6** + 抬头 `D2–D19`→`D2–D20` + §27.5 遗留 ① 划掉；`qa_report.md` 新增**第 21 项** + 抬头二十→**二十一处** + 第 20 项遗留 ① 划掉 + §1/§6.2 基线与记账同步；`prompt.md` 把「6.3 与 6.4 都照不到这 21 条」的前瞻注换成 6.5 实况、指针扩到 §27.6、两个种类数口径补 D20 复测值（171 / 225 都不动）；D19 接手卡就地更新到 D20（§0/§3/§4/§5①/§8 第十一条）。记忆两处同步。
- **下一项（待拍板）**：② 丙档 **85 条**两栏矛盾属重采（= 规则 6.3 现在的全部命中，也是它升 ERROR 的唯一阻塞项）；其后 ③ 来源类型缺失 **1305**、④ 该栏仍不收敛（**225 种**）。
