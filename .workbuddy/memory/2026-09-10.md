# 2026-09-10（D38）

## 做了什么
- 用户拍板：**增量 A（补新模型）与 B（D32 字段缺口）都暂缓**，更新频率改为**用户触发**，不设定时任务。
- 新增跟踪源实测：OpenRouter API（`openrouter.ai/api/v1/models`，HTTP 200 / 435 模型）+ HF API（`huggingface.co/api/models`，200）。**两者均可用，OpenRouter 收益最大**（带 `created` 上架日、`context_length`、`pricing`、`hugging_face_id`）。
- 固化 `docs/跟踪源清单.md`（网站 A–D 四层 + 厂商 P0/P1/P2 + 日期口径铁律 + 本机 curl 坑），并在 `docs/增量更新工作流.md` S0 挂接；`docs/README.md` 索引已更新。
- 查证两个"查不到"的模型（结论：都存在，主库都缺）：
  - **Qwen3.8-Flash**：2026-08-26 官方发布 + 开源，125B+51B N-gram MoE / 激活 6B / 262K 原生(YaRN 1M)，HF `Qwen/Qwen3.8-Flash-Next`。
  - **LongCat 2.0（美团）**：2026-06-30 发布、07-12 开源，1.6T/48B MoE、1M ctx；"2 系列"只有它一个成员（FP8/INT8 是量化变体）。主库仅 2 条 meituan 记录（LongCat-Flash 2025 年那批）。
- 生成 `temp/d38_wave2_candidates.jsonl`（16 条，NEW 14 / CHECK 1 / EXISTS 1）+ 脚本 `temp/d38_wave2_candidates.py`，**待用户拍板后开采**。
- 出方案（未动数据）：`docs/数据增强方案_跑分与日期.md`。

## 关键实测数字（下次直接引用）
- 主库 900 条：235 条（26.1%）无任何跑分；independent 310（34.4%）；arena 172（19.1%）；`release_date` 只到月 532 条（59.1%）。
- epoch 本地 CSV：75 文件 / **2,417 行** / 619 模型 → 主库精确匹配 **188 条（20.9%）**，其中 34 条无 independent。
  ⚠ **列名不统一**（`Percent correct`/`EM`/`Score`/`Accuracy`…），只认一种会漏 97% 数据。
- `epoch_ai_models` 的 `Publication date`：匹配 453 条，可把 318 条"只到月"精确化；**但有错值**（deepseek-chat 主库 2024-12 vs epoch 2025-12-01），必须抽样校验后再批量写。
- 探针脚本：`temp/d38_epoch_coverage_probe.py`。

## 坑
- `candidate_diff.py` 会把"同家族不同档位"误判为 EXISTS（Qwen3.8 Flash → 匹配到 qwen-3-8-max，overlap 1.0）。**EXISTS 结论遇同家族必须人工复核**（与 Wave-1 的 Qwen3.7-Plus 思考档同一类）。
- OpenRouter `created` = 上架日 ≠ 发布日（LongCat-2.0 发布 06-30 / 上架 07-20，滞后 20 天）；HF `createdAt` 常早于发布日（Qwen3.8-Flash-Next 建库 08-24 / 发布 08-26）。已写入清单 §4 铁律。
- Wave-1 遗留：`Claude Mythos 5.1` 未入库；`cohere:parse-v5-0:base`（文档解析工具）入库与 S1 非模型判定矛盾，待复核。
- 日期精确化要区分"发布日/开源日/上架日/论文日"，阿里云百炼《模型上下架与更新》台账页是官方带日期的范本（已验证）。

## 下午执行（用户拍板后）
拍板项：日期精度用 notes 标注 / Source link 空降 T3 / Wave-2 16 条开采 / 移除 cohere:parse-v5-0:base / 补 34 条无 independent / 顺序=校验→跑分→台账页。

- **主库 900 → 915 条，门禁 ERROR 0 / WARN 0 全程保持**。
- epoch 导入：补 independent **30 条**（非 34 —— 4 条无任何来源 URL，门禁要求 source_url 非空直接拦下）；`release_date` 由月补日 **298 条**（严格判据：epoch 日期唯一 + 落同月）。
  - 关键发现：epoch CSV 的 `Source link` 为空时，**URL 常藏在 `Source` 列**（artificialanalysis.ai / arcprize.org / cursor.com 等真实榜单）。只认 Source link 会误判 46% 无来源。
  - ⚠ "Source link 空降 T3"这条拍板，落到实现上是：Source link 有→T1+该域名作 source_site；Source link 空但 Source 列有 URL→T3+该域名；两者都空→**不导入**（门禁不让）。
- Wave-2 16 条全部采集入库（批次 b319w2~b328w2，4 个 subagent 并行：A 4/B 5/C 4/D 3），单文件门禁全 ERROR 0。
  - 合并后出现 WARN 1（bytedance:seed-2-0-code 有定价缺 effective_date），补 2026-09-10 + notes 说明后回 0。**基线 WARN 0 的规矩要守住**。
- 台账页映射表：17 个 URL 批量实测，200 的有 Anthropic release-notes、DeepSeek news、Moonshot、Zhipu bigmodel、腾讯混元、美团 tech、Google blog、阿里云；OpenAI 403 反爬；ai.google.dev / ai.meta.com / mistral.ai / docs.x.ai 连接失败待复测；MiniMax、NVIDIA 的 URL 404 需重找。
- 复盘：independent 覆盖 34.4% → 37.2%；到日日期 368 → 665；**覆盖最差的大厂：NVIDIA 16%、IBM 7%、Google 40%、Microsoft 43%**（下一步补分重点）；完全无跑分最多：Alibaba 16 / IBM 10 / allenai 9 / Mistral 7。

## ⚠ 遗留争议（已结案，见 D39）
**`cohere:parse-v5-0:base` 移除可能移错了**。CHANGELOG 里 D37 Wave-2 二查的原文结论是「Cohere Parse 5 查实为 **2.3B 文档 VLM（真模型）**，按页计价 $1.5/千页」，而本轮按 Wave-1 S1 的旧判"疑似文档解析工具"移除。两者冲突，且记录内架构名是 `Proprietary north-micro-vision-instruct`（VLM）。
回滚脚本已备好：`temp/d38_restore_cohere_parse.py`（未执行）。用户拍板时不知道 D37 这条证据，需重新确认。

---

# 2026-09-10（D39，当天第二轮）

## 拍了什么板
- **Cohere Parse 5 终裁：维持移除**。用户理由：**计费方式与主流不符**（按页计价 $1.50/1,000 页，token 六键全 null），不像常规模型。
  - 关键口径：D37"2.3B 文档 VLM＝真模型"针对的是"是否存在/是否为模型"，与"是否符合本库收录口径"是**两个层次**的问题，不构成保留依据。这句逻辑要记住，以后同类争议照此分层。
  - 已落地：CHANGELOG [D39] Removed + D38 段"存争议"改为已终裁；`docs/non_model_records.jsonl` 第 8 条 meta.notes 写入终裁；`temp/d38_restore_cohere_parse.py` 标注 **VOID 作废**（保留历史留档，不删）。
- 下一步重点＝**arena 跑分补全**，已执行完毕。

## arena 补全（D39 主目标，已完成）
- **DataLearner 是 arena 唯一可用源**（lmarena.ai 本机 HTTP 000 不可达）：
  - 主榜 **有 JSON API**：`https://www.datalearner.com/api/leaderboards/external/text-generation`（免鉴权，400 行，含 rank/modelCode/score/ci/votes/organization/license/thinkingMode）。
    ⚠ 端点**不在导航里**，藏在榜单页 `<script type="application/ld+json">` 的 `distribution.contentUrl`。
  - 分类榜（coding 395 / math 383）**无 API（404）**，数据在 Next.js App Router 的 flight 流（`self.__next_f.push([1,"…"])`）。⚠ `grep "elo"` 会 0 命中而误判"无数据"。
  - 图像/视频 4 个榜已探到路径（image-edit / text-to-image / image-to-video / video-generation），未采。
- **结果**：补入 296 条 arena 条目 / 覆盖 100 个模型（**43 个此前完全没有 arena**）。arena 172（19.1%）→ **215（23.5%）**；条目 513 → **809**。门禁 ERROR 0 / WARN 0（915 条不变）。
- **三道保守过滤**（写进导入脚本，同类任务照抄）：
  1. 榜单重名（同类别同名多条，如 Claude 3.5 Sonnet 1374/1343）→ 跳过 33
  2. 版本差异（同一榜单条目被多个主库记录共用且非仅上下文差异，如 GPT-4 被 0314/0613/1106/0125 共用）→ 跳过 63
  3. 放行：仅上下文尺寸差异（去掉 `-32k/-64k` 后相同）→ 6
- 变体口径：主库无 thinking 标记 ⇒ 只取榜单「无变体标记」条目；有 ⇒ 才取 `(thinking)/(high)/(xhigh)`。
- **追加而非覆盖**（沿用 qwen-3-8-max 先例，靠 date 区分快照）。⚠ 副作用：条目 513→809，用户若要瘦身可另起一轮。
- ⚠ 覆盖没"翻倍"——瓶颈不是抓取，而是**主库大量模型根本没上过 LMArena**（长尾/小厂/base 模型）。

## 意外高价值产出：榜单反推漏采
- 榜单 236 条未被主库命中 → **84 条疑似真漏采**，已转 S0 格式 `temp/d40_wave1_candidates.jsonl`；`candidate_diff.py` 分档 **NEW 59 / CHECK_VARIANT 11 / EXISTS 14**。
- 高价值：GPT-5.2 Chat / GPT-5.5 Instant / GPT-5.3 Chat、Qwen3.5/3.6/3.7-Max-Preview（阿里）、ERNIE-5.1-Preview / ERNIE 5.0 Preview（百度）、Grok 4.1 Thinking、DOLA Seed 2.0 Pro（字节）、Kimi K2.5 Instant、GLM-5V-Turbo、mimo-v2-flash（小米）、hunyuan-vision-1.5-thinking（腾讯）、OpenAI o1/o3/o4-mini。
- ⚠ 不可直接采：榜单含匿名实验版代号（amazon-nova-experimental-chat-26-02-10、GPT-5-Pro）；老社区模型（vicuna/alpaca/oasst-pythia/GPT4All）虽在榜且主库无，但未必符合"主流大模型"口径，需人工筛。
- 另 152 条"疑似库内已有但命名不同"**difflib 不可信**（把 `Claude Opus 5` 错配到 `claude-opus-4-5-...`），只能当线索。

## 遗留
- arena 快照瘦身（513→809 条目）待定。
- `independent` 仍 37.2%；Artificial Analysis `api/v2/data/llms/models` 返回 **401 需 key**，未接入；OpenCompass / SuperCLUE 待验证。
- 自检发现 3 处**旧** arena 条目缺 `source_type`/`confidence`（gemini-3-6-flash-high、muse-spark ×2）——属 D32 存量字段缺口（A 类），未动。

## 产物
- 文档：`docs/D39_ARENA_GAP_REPORT.md`（新建）、`docs/跟踪源清单.md` §C-1（DataLearner 抓取要点）、`docs/数据增强方案_跑分与日期.md`（L1 标记已做）、`docs/README.md`（索引）
- **生产脚本已固化到 `scripts/`（不放 temp，temp 会被清理）**：
  - `scripts/d39_fetch_arena_leaderboard.py` —— 抓三榜 + 主榜 API meta → `temp/d39_arena_raw.json`
  - `scripts/d39_import_arena_elo.py` —— 导入主库，含三道过滤，缺省 dry-run，`--apply` 才写，**幂等**
  - 下次刷新 arena 只需这两条命令
- 调试脚本留 temp：`d39_dl_tables.py`、`d39_arena_match2.py`、`d39_arena_audit_1ton.py`、`d39_gap_from_arena.py`、`d39_make_d40_candidates.py`、`d39_finalize_cohere.py`
- 备份：`backups/model_data_v2.pre-d39-arena-20260910-151430.jsonl`


---

# D40 轮（2026-09-10 下午）arena 瘦身 + AA 接入 + Wave-1 增量 22 条

用户拍板原文：「1.是 2.是 3.看看能不能浏览器去连 4. AB的采集也可以一起做了」= ① arena 瘦身 是 ② D40 候选开采 是 ③ 试浏览器连 AA ④ A+B 一起做。
**主库 915 → 937 条（+22），门禁 ERROR 0 / WARN 0。**

## ③ AA「浏览器去连」——实测不需要浏览器（重要）
- AA **API** `/api/v2/data/llms/models` 确返 **401（需 key）**；但**榜单页 `artificialanalysis.ai/leaderboards/models` 200 / 2.75 MB，644 模型全部评测字段内嵌在 Next.js flight 流**（`self.__next_f.push([1,"…"])`）。`curl --ssl-no-revoke` 直读即可，**不必装 agent-browser（≈500MB Chromium）**。
- 生产脚本 `scripts/d40_fetch_artificial_analysis.py`；产物 `temp/d40_aa_models.json`（735 条，644 带 releaseDate、633 带 intelligenceIndex）。
- **导入口径（已定死）**：仅精确匹配（270 对）；仅补 `independent` 空值；**复合指数不导**（intelligenceIndex/omniscience 量程 3.75–53.37，非 0–1）；releaseDate 只做月→日精化。
- ⚠ 模糊匹配翻车：把 `command-a-plus` 误配 `qwen-plus`——根因是把 `command`/`gemma`/`gemini`/`phi`/`olmo`/`jamba`/`nemotron`/`lfm` 当 vendor 前缀剥掉（它们是**系列名不是厂牌**）。**vendor 前缀表只能放真厂牌。**
- 结果：99 条记录 / **+913 条 independent 条目**，43 条日期精化。`independent` 覆盖 340(37.2%) → **450(48.0%)**。

## ① arena 瘦身
809 → 633 条（同 `(model_id, sub_benchmark)` 只留最新 date）；`is_primary` 归一（**仅 text 为 true**——初版误把全部缺失值置 true，库内惯例是 text True / coding·math 全 False）。

## ④B Wave-1 增量 22 条（4 批次 b329w1~b332w1，全 submitted）
- OpenAI 系 6 / 阿里 5 / 腾讯 5 / DeepSeek+小米+美团+字节 6。
- **7 个文件曾报门禁错，逐类修完**：
  - `source_type` 枚举违规 25 处（agent 自造 `官方发布页（自报）`/`行业媒体转述官方发布`/`LMArena`）→ 按库惯归正（`T0-自报-转述` ↔ `行业媒体转述官方发布（自报）`，169 例）
  - arena 段 5 组来源虚假（url 指新闻站/上游 arena.ai，rank/votes/ci 塞在 notes）→ **整段用 DataLearner 本地快照 2026-09-02 权威值重建**
  - `gemini-pro-dev-api` release_date='2024' → 查 Google 开发者博客定为 **2023-12-13**（Gemini API 面向开发者开放日），状态改「已定死」
  - `long_context` 键名漂移 `input_multiplier`→规范键 `input`/`output`
  - 6 条缺 `pricing.effective_date`（库基线 298/298 全有）→ 锚官方发布/版本标签日，**仅知月用 YYYY-MM + notes 注明周期，不伪造到日**
  - `qwen-3-6-plus-preview` → `qwen3-6-plus-preview`（库内 `qwen3-*` 紧贴 36 : 连字符 3）
- ⚠ **采集 agent 写的 arena 分值有偏差**：`qwen3-7-max-preview` coding 记 1526（新闻），DataLearner 真值 **1525**；`qwen3-5-max-preview` text 记 1464，真值 **1466**。**榜单分必须以镜像站结构化数据为准，新闻转述只当线索。**

## ④A A 类历史缺口
D32 五项中 #2/#3/#4 已在 D33–D37 修完（0/0/1）；#5 license 本轮治理：**改走 HuggingFace API**（`huggingface.co/api/models/{repo}` 的 tags 里读 `license:`）作权威源，取代正则抽文本（正则曾误判 `HF`、`Under the MIT`）；配套**同尺寸一致性校验**（repo 与记录参数量 token 交集空则拒写），拦下 `g42:jais-70b` ← `inception42/jais-13b`。写入 36 条，填充率 51% → **56%**；余 46 条空白（其中 32 条无 HF repo）。

## 合并后收尾三自检（全过）
数组缩水取证（915 老记录 × 5 数组字段）无缩水 / 撞键分类（22 新记录）无重复键 / WARN 基线 0→0。备份 `backups/model_data_v2.pre-d40-wave1-merge-20260910-160906.jsonl`。

## 遗留待拍板（详见 docs/D40_REPORT.md §七）
1. **5 条 arena 段位错置**（search / LiveCodeBench Pro / gdpval / agent）来源与段位语义矛盾，需用户裁定迁移或移除；
2. `bytedance:dola-seed-2-0-pro:base` 信息过薄（仅 arena，无日期/定价/架构，AA+OR 均无）；
3. vendor 大小写混乱（Google/google、Alibaba 三写法，A7: 85 条）；
4. Qwen 命名双轨（qwen3-* 36 vs qwen-3-* 3）；
5. license 余 46 条空白。

## 产物
- 报告 `docs/D40_REPORT.md`（新建）；`CHANGELOG.md` [D40]；`docs/跟踪源清单.md` §C 新增 AA + §C-2；`docs/README.md` 索引
- 生产脚本固化 `scripts/`：`d40_fetch_artificial_analysis.py` · `d40_aa_import.py` · `d40_license_hf.py` · `d40_postcheck.py`（收尾三自检，可复用）
- 采集原件留档 `temp/d40_incoming_orig/`（22 个，归一前）
