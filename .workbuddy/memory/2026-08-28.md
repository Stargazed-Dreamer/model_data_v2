# 2026-08-28 工作日志

## 落盘 WorkBuddy agent 专属作业规范（agent: workbuddy-02）

新建 `docs/WORKBUDDY_AGENT_GUIDE.md`，作为 `multi_platform_subagent_guide.md` 的平台补丁。核心内容：
- 红线 A：必须用用户本机 git（`/e/System_Programes/Git/cmd/git` v2.46.0，helper=manager）而非 WorkBuddy 自带 PortableGit（v2.55.0，helper=helper-selector，push 弹窗卡死）
- 红线 B：`incoming/models/*.jsonl` 被 .gitignore 忽略，提交必须 `git add -f`
- 防烂尾接管规则 + 会话结束前强制自检（判定完成的唯一标准：ledger 改完 + push 成功 + `git ls-files` 能查到）

## 开工成果（本轮共推送 48 个文件入库）

1. **收尾 workbuddy-01 烂尾**：b9w6/b9w7-openai、b12w4-anthropic 共 15 个文件早已落盘（门禁 ERROR=0）但从未更新账本、从未入库。接管并 submitted。
2. **补录漏 `add -f` 的文件**：b9w2/b9w4-openai、b10w2-google、b12w2-anthropic、b14w2-nvidia 共 25 个文件，账本早已 submitted 但文件从未入库。
3. **推送 11 个历史堆积 commit**（trae-cn-glmm 的 submit，标注 local/no-push）。本机 git 一次性静默推送成功，验证红线 A 有效。
4. **新采集 2 批 8 个模型**：b45w1-perplexity（sonar-pro / sonar-deep-research / sonar-reasoning-pro / r1-1776）、b46w1-pleias（pleias-1-0-350m / pleias-1-0-1-2b / pleias-rag-350m / pleias-rag-1b），门禁全部 ERROR=0。

## 关键排查结论

- 82 个 submitted 批次应产出 358 个文件，其中 262 个不在磁盘。**逐一比对主库后确认 262 个全部已在 `model_data_v2.jsonl`（950 条记录）中，0 真丢失**，属「采集→合并→文件未保留」的正常链路。以后判断文件是否真丢，先查主库 model_id 集合。
- 5 个 claimed 未交付批次中，b9w6/b9w7/b12w4（workbuddy-01，超 24h stale）+ b44w1（trae，文件齐全）属可收尾；b33w1（trae，0 文件）真正未开工。
- 决策：未超时（<24h）的他人 claimed 批次一律不抢。后续验证正确 —— trae 在 claim 后约 1 小时内自行提交了 b33w1(5/5) 与 b44w1(4/4)。

## 当前库状态（2026-08-28 收工）

submitted 89 / claimed 2（他人进行中）/ pending 214。工作区干净，本地与远程 origin/main 完全同步（0/0）。主库 model_data_v2.jsonl 全程未改动。
