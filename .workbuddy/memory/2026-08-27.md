# 2026-08-27

## model_data v2 多平台协作项目
- 按 `docs/multi_platform_subagent_guide.md` 开工。认领批次 `b9w5-openai`（5 个 OpenAI 模型）。
- 排查并解决 git push 弹窗：根因是 WorkBuddy 自带 PortableGit 的 `credential.helper=helper-selector` 不读 Windows 凭据；改用用户自己装的 Git（`E:\System_Programes\Git\cmd\git`，`manager`）后零弹窗 push 成功，认领已推上 origin（commit 9b9c256）。
- 派 5 个采集 subagent 后台并行采集 b9w5-openai 的模型：gpt-5-2-2025-12-11-xhigh / gpt-5-4-mini-2026-03-17-xhigh / gpt-5-5-low / gpt-5-6-luna-none / gpt-5-6-terra-none。
- 待 5 个档案落盘后：主 agent 验收（validate ERROR=0）→ 更新 ledger 为 submitted → 用用户 git 二进制 push。
- 约定：本会话所有 git push 一律调用用户 git（`/e/System_Programes/Git/cmd/git`），避免弹窗。

## b9w5-openai 已完成并提交（commit ca2404c）
- 5 个模型档案全部落盘、validate ERROR=0。
- 踩坑并修正：① 模型文件被 `.gitignore` 第29行忽略，须 `git add -f` 才进仓库（已强制 add）；② 文件名须 `batch__vendor__model__base` 双下划线规范（已重命名）；③ `model_id` variant 以 ledger 的 models 字段为准（多为 base），已把误拆的 variant 改回 base。
- 最终文件：`b9w5-openai__openai__gpt-5-2-2025-12-11-xhigh__base.jsonl` 等 5 个，ledger=submitted，已 push 至 origin。
